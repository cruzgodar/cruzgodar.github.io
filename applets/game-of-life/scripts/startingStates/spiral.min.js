import{writeStateFromAscii}from"./writeStateFromArray.min.js";function spiral(gridSize){gridSize=Math.max(gridSize,100);var i=new Uint8Array(gridSize*gridSize);return writeStateFromAscii({state:i,gridSize:gridSize,ascii:`
		.o..
		.ooo
		.ooo
		oooo
	`}),[i,gridSize]}export{spiral};