function verticalLine(gridSize){var i=new Uint8Array(gridSize*gridSize),r=Math.floor((gridSize-1)/2),e=Math.ceil((gridSize-1)/2);for(let g=0;g<i.length;g++){var t=g%gridSize;i[g]=t===r||t===e?1:0}return[i,gridSize]}export{verticalLine};