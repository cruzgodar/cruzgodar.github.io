import{writeStateFromAscii}from"./writeStateFromArray.min.js";function acorn(gridSize){gridSize=Math.max(gridSize,300);var i=new Uint8Array(gridSize*gridSize);return writeStateFromAscii({state:i,gridSize:gridSize,ascii:`
		.o.....
		...o...
		oo..ooo
	`}),[i,gridSize]}export{acorn};