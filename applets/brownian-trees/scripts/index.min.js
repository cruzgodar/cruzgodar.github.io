"use strict";!async function(){await Site.loadApplet("brownian-trees");let t=new BrownianTree($("#output-canvas"));function e(){let e=parseInt(a.value||1e3);t.run(e)}let n=$("#generate-button");n.addEventListener("click",e);let a=$("#resolution-input");t.listenToInputElements([a],e);let o=$("#download-button");o.addEventListener("click",()=>wilson.downloadFrame("a-brownian-tree.png")),Page.show()}();