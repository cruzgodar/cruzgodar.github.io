"use strict";!async function(){await Site.loadApplet("brownian-trees");let e=new BrownianTree($("#output-canvas"));function t(){let t=parseInt(l.value||1e3);e.run(t)}let n=$("#generate-button");n.addEventListener("click",t);let l=$("#resolution-input");e.listenToInputElements([l],t);let o=$("#download-button");o.addEventListener("click",()=>wilson.downloadFrame("a-brownian-tree.png")),Page.show()}();