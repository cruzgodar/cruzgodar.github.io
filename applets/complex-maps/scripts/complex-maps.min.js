!function(){"use strict";let t=null,n=null,e=1,r=1,o=[],c=500,a=100,l=0,i=0,u=0,w=0,s=0,z=0,f=0,d=0;const v=.96,_=.0025,g=25e-5,p=.93,m=.01,h=.001;let x=-1,y=document.querySelector("#code-textarea");y.value="cexp(cinv(z))",document.querySelector("#generate-button").addEventListener("click",q);let b=document.querySelector("#resolution-input");b.addEventListener("input",()=>{c=parseInt(b.value||500),t.change_canvas_size(c,c),window.requestAnimationFrame(L)}),document.querySelector("#download-button").addEventListener("click",()=>{t.download_frame("a-complex-map.png")});let M=document.querySelector("#canvas-location");function q(){let e=`\n\t\t\tprecision highp float;\n\t\t\t\n\t\t\tvarying vec2 uv;\n\t\t\t\n\t\t\tuniform float aspect_ratio;\n\t\t\t\n\t\t\tuniform float world_center_x;\n\t\t\tuniform float world_center_y;\n\t\t\tuniform float world_size;\n\t\t\t\n\t\t\tuniform float brightness_scale;\n\t\t\t\n\t\t\tconst vec2 i = vec2(0.0, 1.0);\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\t//Returns |z|.\n\t\t\tfloat cabs(vec2 z)\n\t\t\t{\n\t\t\t\treturn length(z);\n\t\t\t}\n\t\t\t\n\t\t\tfloat cabs(float z)\n\t\t\t{\n\t\t\t\treturn abs(z);\n\t\t\t}\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\t//Returns |z|.\n\t\t\tfloat carg(vec2 z)\n\t\t\t{\n\t\t\t\tif (z.x == 0.0)\n\t\t\t\t{\n\t\t\t\t\tif (z.y >= 0.0)\n\t\t\t\t\t{\n\t\t\t\t\t\treturn 1.57079632;\n\t\t\t\t\t}\n\t\t\t\t\t\n\t\t\t\t\treturn -1.57079632;\n\t\t\t\t}\n\t\t\t\t\n\t\t\t\treturn atan(z.y, z.x);\n\t\t\t}\n\t\t\t\n\t\t\tfloat carg(float z)\n\t\t\t{\n\t\t\t\tif (z >= 0.0)\n\t\t\t\t{\n\t\t\t\t\treturn 0.0;\n\t\t\t\t}\n\t\t\t\t\n\t\t\t\treturn 3.14159265;\n\t\t\t}\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\t//Returns the conjugate of z.\n\t\t\tvec2 cconj(vec2 z)\n\t\t\t{\n\t\t\t\treturn vec2(z.x, -z.y);\n\t\t\t}\n\t\t\t\n\t\t\tfloat cconj(float z)\n\t\t\t{\n\t\t\t\treturn z;\n\t\t\t}\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\t//Returns z / |z|.\n\t\t\tvec2 csign(vec2 z)\n\t\t\t{\n\t\t\t\tif (length(z) == 0.0)\n\t\t\t\t{\n\t\t\t\t\treturn vec2(0.0, 0.0);\n\t\t\t\t}\n\t\t\t\t\n\t\t\t\treturn z / length(z);\n\t\t\t}\n\t\t\t\n\t\t\tfloat csign(float z)\n\t\t\t{\n\t\t\t\treturn sign(z);\n\t\t\t}\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\t//Returns z + w.\n\t\t\tvec2 cadd(vec2 z, vec2 w)\n\t\t\t{\n\t\t\t\treturn z + w;\n\t\t\t}\n\t\t\t\n\t\t\tvec2 cadd(vec2 z, float w)\n\t\t\t{\n\t\t\t\treturn vec2(z.x + w, z.y);\n\t\t\t}\n\t\t\t\n\t\t\tvec2 cadd(float z, vec2 w)\n\t\t\t{\n\t\t\t\treturn vec2(z + w.x, w.y);\n\t\t\t}\n\t\t\t\n\t\t\tfloat cadd(float z, float w)\n\t\t\t{\n\t\t\t\treturn z + w;\n\t\t\t}\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\t//Returns z - w.\n\t\t\tvec2 csub(vec2 z, vec2 w)\n\t\t\t{\n\t\t\t\treturn z - w;\n\t\t\t}\n\t\t\t\n\t\t\tvec2 csub(vec2 z, float w)\n\t\t\t{\n\t\t\t\treturn vec2(z.x - w, z.y);\n\t\t\t}\n\t\t\t\n\t\t\tvec2 csub(float z, vec2 w)\n\t\t\t{\n\t\t\t\treturn vec2(z - w.x, -w.y);\n\t\t\t}\n\t\t\t\n\t\t\tfloat csub(float z, float w)\n\t\t\t{\n\t\t\t\treturn z - w;\n\t\t\t}\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\t//Returns z * w.\n\t\t\tvec2 cmul(vec2 z, vec2 w)\n\t\t\t{\n\t\t\t\treturn vec2(z.x * w.x - z.y * w.y, z.x * w.y + z.y * w.x);\n\t\t\t}\n\t\t\t\n\t\t\tvec2 cmul(vec2 z, float w)\n\t\t\t{\n\t\t\t\treturn z * w;\n\t\t\t}\n\t\t\t\n\t\t\tvec2 cmul(float z, vec2 w)\n\t\t\t{\n\t\t\t\treturn z * w;\n\t\t\t}\n\t\t\t\n\t\t\tfloat cmul(float z, float w)\n\t\t\t{\n\t\t\t\treturn z * w;\n\t\t\t}\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\t//Returns z / w.\n\t\t\tvec2 cdiv(vec2 z, vec2 w)\n\t\t\t{\n\t\t\t\tif (length(w) == 0.0)\n\t\t\t\t{\n\t\t\t\t\treturn vec2(1.0, 0.0);\n\t\t\t\t}\n\t\t\t\t\n\t\t\t\treturn vec2(z.x * w.x + z.y * w.y, -z.x * w.y + z.y * w.x) / (w.x * w.x + w.y * w.y);\n\t\t\t}\n\t\t\t\n\t\t\tvec2 cdiv(vec2 z, float w)\n\t\t\t{\n\t\t\t\tif (w == 0.0)\n\t\t\t\t{\n\t\t\t\t\treturn vec2(1.0, 0.0);\n\t\t\t\t}\n\t\t\t\t\n\t\t\t\treturn z / w;\n\t\t\t}\n\t\t\t\n\t\t\tvec2 cdiv(float z, vec2 w)\n\t\t\t{\n\t\t\t\tif (length(w) == 0.0)\n\t\t\t\t{\n\t\t\t\t\treturn vec2(1.0, 0.0);\n\t\t\t\t}\n\t\t\t\t\n\t\t\t\treturn vec2(z * w.x, -z * w.y) / (w.x * w.x + w.y * w.y);\n\t\t\t}\n\t\t\t\n\t\t\tfloat cdiv(float z, float w)\n\t\t\t{\n\t\t\t\tif (w == 0.0)\n\t\t\t\t{\n\t\t\t\t\treturn 1.0;\n\t\t\t\t}\n\t\t\t\t\n\t\t\t\treturn z / w;\n\t\t\t}\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\t//Returns 1/z.\n\t\t\tvec2 cinv(vec2 z)\n\t\t\t{\n\t\t\t\tfloat magnitude = z.x*z.x + z.y*z.y;\n\t\t\t\t\n\t\t\t\treturn vec2(z.x / magnitude, -z.y / magnitude);\n\t\t\t}\n\t\t\t\n\t\t\tfloat cinv(float z)\n\t\t\t{\n\t\t\t\tif (z == 0.0)\n\t\t\t\t{\n\t\t\t\t\treturn 1.0;\n\t\t\t\t}\n\t\t\t\t\n\t\t\t\treturn 1.0 / z;\n\t\t\t}\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\t//Returns z^w.\n\t\t\tvec2 cpow(vec2 z, vec2 w)\n\t\t\t{\n\t\t\t\tfloat arg = carg(z);\n\t\t\t\tfloat magnitude = z.x * z.x + z.y * z.y;\n\t\t\t\t\n\t\t\t\tfloat exparg = exp(-w.y * arg);\n\t\t\t\tfloat magexp = pow(magnitude, w.x / 2.0);\n\t\t\t\tfloat logmag = log(magnitude) * w.y / 2.0;\n\t\t\t\t\n\t\t\t\tfloat p1 = exparg * cos(w.x * arg);\n\t\t\t\tfloat p2 = exparg * sin(w.x * arg);\n\t\t\t\t\n\t\t\t\tfloat q1 = magexp * cos(logmag);\n\t\t\t\tfloat q2 = magexp * sin(logmag);\n\t\t\t\t\n\t\t\t\treturn vec2(p1 * q1 - p2 * q2, q1 * p2 + p1 * q2);\n\t\t\t}\n\t\t\t\n\t\t\tvec2 cpow(vec2 z, float w)\n\t\t\t{\n\t\t\t\tfloat arg = carg(z);\n\t\t\t\tfloat magnitude = z.x * z.x + z.y * z.y;\n\t\t\t\t\n\t\t\t\tfloat magexp = pow(magnitude, w / 2.0);\n\t\t\t\t\n\t\t\t\tfloat p1 = cos(w * arg);\n\t\t\t\tfloat p2 = sin(w * arg);\n\t\t\t\t\n\t\t\t\treturn vec2(p1 * magexp, p2 * magexp);\n\t\t\t}\n\t\t\t\n\t\t\tvec2 cpow(float z, vec2 w)\n\t\t\t{\n\t\t\t\tif (z == 0.0)\n\t\t\t\t{\n\t\t\t\t\treturn vec2(0.0, 0.0);\n\t\t\t\t}\n\t\t\t\t\n\t\t\t\tfloat zlog = log(z);\n\t\t\t\tfloat zexp = exp(w.x * zlog);\n\t\t\t\t\n\t\t\t\treturn vec2(zexp * cos(w.y * zlog), zexp * sin(w.y * zlog));\n\t\t\t}\n\t\t\t\n\t\t\tfloat cpow(float z, float w)\n\t\t\t{\n\t\t\t\treturn pow(z, w);\n\t\t\t}\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\t//Returns z^^w.\n\t\t\tvec2 ctet(vec2 z, float w)\n\t\t\t{\n\t\t\t\tif (w == 0.0)\n\t\t\t\t{\n\t\t\t\t\treturn vec2(1.0, 0.0);\n\t\t\t\t}\n\t\t\t\n\t\t\t\t\n\t\t\t\tvec2 prod = z;\n\t\t\t\t\n\t\t\t\tfor (int i = 1; i < 10000; i++)\n\t\t\t\t{\n\t\t\t\t\tif (float(i) >= w)\n\t\t\t\t\t{\n\t\t\t\t\t\treturn prod;\n\t\t\t\t\t}\n\t\t\t\t\t\n\t\t\t\t\tprod = cpow(prod, z);\n\t\t\t\t}\n\t\t\t\t\n\t\t\t\treturn prod;\n\t\t\t}\n\t\t\t\n\t\t\tfloat ctet(float z, float w)\n\t\t\t{\n\t\t\t\tif (w == 0.0)\n\t\t\t\t{\n\t\t\t\t\treturn 1.0;\n\t\t\t\t}\n\t\t\t\t\n\t\t\t\t\n\t\t\t\t\n\t\t\t\tfloat prod = z;\n\t\t\t\t\n\t\t\t\tfor (int i = 1; i < 10000; i++)\n\t\t\t\t{\n\t\t\t\t\tif (float(i) >= w)\n\t\t\t\t\t{\n\t\t\t\t\t\treturn prod;\n\t\t\t\t\t}\n\t\t\t\t\t\n\t\t\t\t\tprod = pow(prod, z);\n\t\t\t\t}\n\t\t\t\t\n\t\t\t\treturn prod;\n\t\t\t}\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\t//Returns sqrt(z).\n\t\t\tvec2 csqrt(vec2 z)\n\t\t\t{\n\t\t\t\treturn cpow(z, .5);\n\t\t\t}\n\t\t\t\n\t\t\tvec2 csqrt(float z)\n\t\t\t{\n\t\t\t\tif (z >= 0.0)\n\t\t\t\t{\n\t\t\t\t\treturn vec2(sqrt(z), 0.0);\n\t\t\t\t}\n\t\t\t\t\n\t\t\t\treturn vec2(0.0, sqrt(-z));\n\t\t\t}\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\t//Returns e^z.\n\t\t\tvec2 cexp(vec2 z)\n\t\t\t{\n\t\t\t\treturn cpow(2.7182818, z);\n\t\t\t}\n\t\t\t\n\t\t\tfloat cexp(float z)\n\t\t\t{\n\t\t\t\treturn exp(z);\n\t\t\t}\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\t//Returns log(z).\n\t\t\tvec2 clog(vec2 z)\n\t\t\t{\n\t\t\t\treturn vec2(.5 * log(z.x * z.x + z.y * z.y), carg(z));\n\t\t\t}\n\t\t\t\n\t\t\tfloat clog(float z)\n\t\t\t{\n\t\t\t\tif (z == 0.0)\n\t\t\t\t{\n\t\t\t\t\treturn 0.0;\n\t\t\t\t}\n\t\t\t\t\n\t\t\t\treturn log(z);\n\t\t\t}\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\t//Returns sin(z).\n\t\t\tvec2 csin(vec2 z)\n\t\t\t{\n\t\t\t\tvec2 temp = cexp(cmul(z, vec2(0.0, 1.0))) - cexp(cmul(z, vec2(0.0, -1.0)));\n\t\t\t\t\n\t\t\t\treturn cmul(temp, vec2(0.0, -0.5));\n\t\t\t}\n\t\t\t\n\t\t\tfloat csin(float z)\n\t\t\t{\n\t\t\t\treturn sin(z);\n\t\t\t}\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\t//Returns cos(z).\n\t\t\tvec2 ccos(vec2 z)\n\t\t\t{\n\t\t\t\tvec2 temp = cexp(cmul(z, vec2(0.0, 1.0))) + cexp(cmul(z, vec2(0.0, -1.0)));\n\t\t\t\t\n\t\t\t\treturn cmul(temp, vec2(0.0, -0.5));\n\t\t\t}\n\t\t\t\n\t\t\tfloat ccos(float z)\n\t\t\t{\n\t\t\t\treturn cos(z);\n\t\t\t}\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\t//Returns tan(z).\n\t\t\tvec2 ctan(vec2 z)\n\t\t\t{\n\t\t\t\tvec2 temp = cexp(cmul(z, vec2(0.0, 2.0)));\n\t\t\t\t\n\t\t\t\treturn cdiv(cmul(vec2(0.0, -1.0), vec2(-1.0, 0.0) + temp), vec2(1.0, 0.0) + temp);\n\t\t\t}\n\t\t\t\n\t\t\tfloat ctan(float z)\n\t\t\t{\n\t\t\t\treturn tan(z);\n\t\t\t}\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\tvec3 hsv2rgb(vec3 c)\n\t\t\t{\n\t\t\t\tvec4 K = vec4(1.0, 2.0 / 3.0, 1.0 / 3.0, 3.0);\n\t\t\t\tvec3 p = abs(fract(c.xxx + K.xyz) * 6.0 - K.www);\n\t\t\t\treturn c.z * mix(K.xxx, clamp(p - K.xxx, 0.0, 1.0), c.y);\n\t\t\t}\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\t//Returns f(z) for a polynomial f with given roots.\n\t\t\tvec2 f(vec2 z)\n\t\t\t{\n\t\t\t\treturn ${y.value||"cexp(cinv(z))"};\n\t\t\t}\n\t\t\t\n\t\t\t\n\t\t\t\n\t\t\tvoid main(void)\n\t\t\t{\n\t\t\t\tvec2 z;\n\t\t\t\t\n\t\t\t\tif (aspect_ratio >= 1.0)\n\t\t\t\t{\n\t\t\t\t\tz = vec2(uv.x * aspect_ratio * world_size + world_center_x, uv.y * world_size + world_center_y);\n\t\t\t\t}\n\t\t\t\t\n\t\t\t\telse\n\t\t\t\t{\n\t\t\t\t\tz = vec2(uv.x * world_size + world_center_x, uv.y / aspect_ratio * world_size + world_center_y);\n\t\t\t\t}\n\t\t\t\t\n\t\t\t\t\n\t\t\t\t\n\t\t\t\tvec2 image_z = f(z);\n\t\t\t\tfloat modulus = length(image_z);\n\t\t\t\t\n\t\t\t\tfloat h = atan(image_z.y, image_z.x) / 6.283;\n\t\t\t\tfloat s = 1.0 / (1.0 + .01 * modulus);\n\t\t\t\tfloat v = 1.0 / (1.0 + .01 / modulus);//max(1.0 / (1.0 + 1.0 / modulus), 1.0 / (1.0 + 1.0 * modulus));\n\t\t\t\t\n\t\t\t\tgl_FragColor = vec4(hsv2rgb(vec3(h, s, v)), 1.0);\n\t\t\t}\n\t\t`,c={renderer:"gpu",shader:e,canvas_width:500,canvas_height:500,world_width:2,world_height:2,world_center_x:0,world_center_y:0,use_fullscreen:!0,true_fullscreen:!0,use_fullscreen_button:!0,enter_fullscreen_button_icon_path:"/graphics/general-icons/enter-fullscreen.png",exit_fullscreen_button_icon_path:"/graphics/general-icons/exit-fullscreen.png",switch_fullscreen_callback:j,mousedown_callback:R,touchstart_callback:R,mousedrag_callback:k,touchmove_callback:k,mouseup_callback:A,touchend_callback:A,wheel_callback:F,pinch_callback:S},a={renderer:"gpu",shader:e,canvas_width:100,canvas_height:100};try{t.output_canvas_container.parentNode.remove(),n.output_canvas_container.parentNode.remove(),M.insertAdjacentHTML("beforebegin",'\n\t\t\t\t<div>\n\t\t\t\t\t<canvas id="output-canvas" class="output-canvas"></canvas>\n\t\t\t\t\t<canvas id="hidden-canvas" class="hidden-canvas"></canvas>\n\t\t\t\t</div>\n\t\t\t')}catch(t){}(t=new Wilson(document.querySelector("#output-canvas"),c)).render.init_uniforms(["aspect_ratio","world_center_x","world_center_y","world_size","brightness_scale"]),(n=new Wilson(document.querySelector("#hidden-canvas"),a)).render.init_uniforms(["aspect_ratio","world_center_x","world_center_y","world_size","brightness_scale"]),o=[],r=-.585,u=0,w=0,s=0,z=0,f=0,d=0,t.gl.uniform1f(t.uniforms.aspect_ratio,1),n.gl.uniform1f(n.uniforms.aspect_ratio,1),window.requestAnimationFrame(L)}function R(t,n,e){z=0,f=0,d=0,u=0,w=0,s=0}function k(n,e,r,o,c){t.world_center_x-=r,t.world_center_y-=o,u=-r/t.world_width,w=-o/t.world_height,window.requestAnimationFrame(L)}function A(t,n,e){Math.sqrt(u*u+w*w)>=_&&(z=u,f=w),Math.abs(s)>=m&&(d=s),window.requestAnimationFrame(L)}function F(t,n,e,o){l=t,i=n,Math.abs(e/100)<.3?r+=e/100:d+=.05*Math.sign(e),K()}function S(n,o,c,a){e>=1?(r-=c/t.world_width*10,s=-c/t.world_width*10):(r-=c/t.world_height*10,s=-c/t.world_height*10),l=n,i=o,K()}function K(){if(e>=1){let n=t.input.get_zoomed_world_center(l,i,3*Math.pow(2,r)*e,3*Math.pow(2,r));t.world_width=3*Math.pow(2,r)*e,t.world_height=3*Math.pow(2,r),t.world_center_x=n[0],t.world_center_y=n[1]}else{let n=t.input.get_zoomed_world_center(l,i,3*Math.pow(2,r),3*Math.pow(2,r)/e);t.world_width=3*Math.pow(2,r),t.world_height=3*Math.pow(2,r)/e,t.world_center_x=n[0],t.world_center_y=n[1]}window.requestAnimationFrame(L)}function L(c){let l=c-x;if(x=c,0===l)return;n.gl.uniform1f(n.uniforms.aspect_ratio,e),n.gl.uniform1f(n.uniforms.world_center_x,t.world_center_x),n.gl.uniform1f(n.uniforms.world_center_y,t.world_center_y),n.gl.uniform1f(n.uniforms.world_size,Math.min(t.world_height,t.world_width)/2),n.gl.uniform1f(n.uniforms.brightness_scale,50),n.render.draw_frame();let i=n.render.get_pixel_data(),u=new Array(a*a);for(let t=0;t<a*a;t++)u[t]=i[4*t]+i[4*t+1]+i[4*t+2];u.sort((t,n)=>t-n);let w=Math.min(Math.max((u[Math.floor(a*a*.96)]+u[Math.floor(a*a*.98)])/50,.5),50);o.push(w),o.length>10&&o.shift(),w=1,t.gl.uniform1f(t.uniforms.aspect_ratio,e),t.gl.uniform1f(t.uniforms.world_center_x,t.world_center_x),t.gl.uniform1f(t.uniforms.world_center_y,t.world_center_y),t.gl.uniform1f(t.uniforms.world_size,Math.min(t.world_height,t.world_width)/2),t.gl.uniform1f(t.uniforms.brightness_scale,w),t.render.draw_frame(),0===z&&0===f&&0===d||(t.world_center_x+=z*t.world_width,t.world_center_y+=f*t.world_height,z*=v,f*=v,Math.sqrt(z*z+f*f)<g&&(z=0,f=0),r+=d,K(),d*=p,Math.abs(d)<h&&(d=0),window.requestAnimationFrame(L))}function j(){t.fullscreen.currently_fullscreen?(e=window.innerWidth/window.innerHeight)>=1?(t.change_canvas_size(c,Math.floor(c/e)),t.world_width=3*Math.pow(2,r)*e,t.world_height=3*Math.pow(2,r)):(t.change_canvas_size(Math.floor(c*e),c),t.world_width=3*Math.pow(2,r),t.world_height=3*Math.pow(2,r)/e):(e=1,t.change_canvas_size(c,c),t.world_width=3*Math.pow(2,r),t.world_height=3*Math.pow(2,r)),window.requestAnimationFrame(L)}q(),window.addEventListener("resize",j),Page.temporary_handlers.resize.push(j)}();