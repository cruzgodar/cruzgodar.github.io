"use strict";!async function(){await Site.loadApplet("kicked-rotator");let e=new KickedRotator($("#output-canvas"));function t(){let t=parseInt(l.value||1e3),n=parseFloat(o.value||.75),u=parseInt(r.value||0);e.run(t,n,u)}let n=$("#generate-button");n.addEventListener("click",t);let l=$("#resolution-input"),o=$("#k-input"),r=$("#orbit-separation-input");e.listenToInputElements([l,o,r],t);let u=$("#download-button");u.addEventListener("click",()=>{e.wilson.downloadFrame("a-kicked-rotator.png")}),Page.show()}();