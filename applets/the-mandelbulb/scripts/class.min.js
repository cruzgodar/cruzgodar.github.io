import anime from"/scripts/anime.min.js";import{magnitude,mat3TimesVector,RaymarchApplet}from"/scripts/applets/raymarchApplet.min.js";class Mandelbulb extends RaymarchApplet{constructor({canvas}){super({canvas:canvas,resolution:400,distanceEstimatorGlsl:`
			vec3 z = pos;
			
			float r = length(z);
			float dr = 1.0;
			
			for (int iteration = 0; iteration < 16; iteration++)
			{
				if (r > 16.0)
				{
					break;
				}
				
				float theta = acos(z.z / r);
				
				float phi = atan(z.y, z.x);
				
				dr = pow(r, power - 1.0) * power * dr + 1.0;
				
				theta *= power;
				
				phi *= power;
				
				z = pow(r, power) * vec3(sin(theta)*cos(phi), sin(phi)*sin(theta), cos(theta));
				
				z += mix(pos, c, juliaProportion);
				
				z = rotationMatrix * z;
				
				r = length(z);
			}
			
			return .5 * log(r) * r / dr;
		`,getColorGlsl:`
			vec3 z = pos;
			
			float r = length(z);
			float dr = 1.0;
			
			vec3 color = vec3(1.0, 1.0, 1.0);
			float colorScale = .5;
			
			for (int iteration = 0; iteration < 100; iteration++)
			{
				if (r > 16.0)
				{
					break;
				}
				
				float theta = acos(z.z / r);
				
				float phi = atan(z.y, z.x);
				
				dr = pow(r, power - 1.0) * power * dr + 1.0;
				
				theta *= power;
				
				phi *= power;
				
				z = pow(r, power) * vec3(sin(theta)*cos(phi), sin(phi)*sin(theta), cos(theta));
				
				z += mix(pos, c, juliaProportion);
				
				z = rotationMatrix * z;
				
				r = length(z);
				
				color = mix(color, abs(z / r), colorScale);
				
				colorScale *= .5;
			}
			
			color /= max(max(color.x, color.y), color.z);
			
			return color;
		`,uniformsGlsl:`
			uniform float power;
			uniform vec3 c;
			uniform float juliaProportion;
			uniform mat3 rotationMatrix;
		`,uniforms:{power:8,c:[0,0,0],juliaProportion:0,rotationMatrix:[[1,0,0],[0,1,0],[0,0,1]]},theta:4.6601,phi:2.272,cameraPos:[.084365,1.91102,1.69388],lightPos:[-10,0,15],lightBrightness:1.25})}distanceEstimator(x,y,z){let o=[x,y,z],t=0,r=1;var a=this.uniforms.c,i=this.uniforms.juliaProportion,e=this.uniforms.power,n=this.uniforms.rotationMatrix;for(let h=0;h<16&&!(16<(t=magnitude(o)));h++){var s=Math.acos(o[2]/t),l=Math.atan2(o[1],o[0]),p=(r=Math.pow(t,e-1)*e*r+1,s*=e,l*=e,Math.pow(t,e));o[0]=p*Math.sin(s)*Math.cos(l)+((1-i)*x+i*a[0]),o[1]=p*Math.sin(s)*Math.sin(l)+((1-i)*y+i*a[1]),o[2]=p*Math.cos(s)+((1-i)*z+i*a[2]),o=mat3TimesVector(n,o)}return.5*Math.log(t)*t/r}switchBulb(instant){const o={t:0},t=this.uniforms.juliaProportion,r=0===this.uniforms.juliaProportion?1:0;anime({targets:o,t:1,duration:instant?10:1e3,easing:"easeOutSine",update:()=>{this.setUniforms({juliaProportion:(1-o.t)*t+o.t*r}),this.needNewFrame=!0}})}downloadFrame(){this.drawFrame(),this.uniforms.juliaProportion<.5?this.wilson.downloadFrame("the-mandelbulb.png",!1):this.wilson.downloadFrame("a-juliabulb.png",!1)}}export{Mandelbulb};