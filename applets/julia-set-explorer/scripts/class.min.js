import{AnimationFrameApplet}from"/scripts/applets/animationFrameApplet.min.js";import{currentlyTouchDevice}from"/scripts/src/interaction.min.js";import{animate,sleep}from"/scripts/src/utils.min.js";import{WilsonGPU}from"/scripts/wilson.min.js";const bubbleRadius=1;class JuliaSetExplorer extends AnimationFrameApplet{wilsonHidden;juliaMode="mandelbrot";numIterations=100;switchJuliaModeButton;ignoreBrightnessCalculation=!1;pastBrightnessScales=[];c=[0,0];resolution=500;resolutionHidden=50;constructor({canvas,switchJuliaModeButton}){super(canvas),this.switchJuliaModeButton=switchJuliaModeButton;var i={shaders:{mandelbrot:`
			precision highp float;
			
			varying vec2 uv;
			
			uniform vec2 worldCenter;
			uniform vec2 worldSize;
			
			uniform int numIterations;
			uniform float brightnessScale;
			
			
			
			void main(void)
			{
				vec2 z = uv * worldSize * 0.5 + worldCenter - vec2(0.75, 0.0);
				
				vec2 c = z;
				
				vec3 color = normalize(
					vec3(
						abs(z.x + z.y) / 2.0,
						abs(z.x) / 2.0,
						abs(z.y) / 2.0
					)
					+ .1 / length(z) * vec3(1.0)
				);
				
				float brightness = exp(-length(z));
				
				
				
				for (int iteration = 0; iteration < 3001; iteration++)
				{
					if (iteration == numIterations)
					{
						gl_FragColor = vec4(0.0, 0.0, 0.0, 1.0);
						return;
					}
					
					if (length(z) >= 4.0)
					{
						break;
					}
					
					z = vec2(z.x * z.x - z.y * z.y, 2.0 * z.x * z.y) + c;
					
					brightness += exp(-length(z));
				}
				
				
				gl_FragColor = vec4(brightness / brightnessScale * color, 1.0);
			}
		`,juliaPicker:`
			precision highp float;
			
			varying vec2 uv;
			
			uniform vec2 worldCenter;
			uniform vec2 worldSize;
			
			uniform vec2 c;
			uniform int numIterations;
			uniform float brightnessScale;
			uniform float juliaRadius;
			uniform float crosshairSize;
			
			
			
			void main(void)
			{
				vec2 z = uv * worldSize * 0.5 + worldCenter - vec2(0.75, 0.0);

				vec2 diffFromC = z - c + vec2(0.75, 0.0);
				float minWorldSize = min(worldSize.x, worldSize.y);

				vec2 minMaxDistanceToCrosshair = vec2(
					min(abs(diffFromC.x), abs(diffFromC.y)),
					max(abs(diffFromC.x), abs(diffFromC.y))
				) / minWorldSize;

				if (minMaxDistanceToCrosshair.x < 0.002 && minMaxDistanceToCrosshair.y < crosshairSize)
				{
					gl_FragColor = vec4(0.75, 0.75, 0.75, 1.0);
					return;
				}

				float distanceFromMouse = clamp(
					length(diffFromC) / minWorldSize * juliaRadius * 10.0,
					0.0,
					1.0
				);

				float t = distanceFromMouse < 0.5
					? 2.0 * distanceFromMouse * distanceFromMouse 
					: 1.0 - (-2.0 * distanceFromMouse + 2.0) * (-2.0 * distanceFromMouse + 2.0) / 2.0;
				
				// Remove the bias as the bubble expands.
				vec2 usableC = mix(c - vec2(0.75, 0.0), z, t);
				
				vec3 color = normalize(
					vec3(
						abs(z.x + z.y) / 2.0,
						abs(z.x) / 2.0,
						abs(z.y) / 2.0
					)
					+ .1 / length(z) * vec3(1.0)
				);
				
				float brightness = exp(-length(z));
				
				
				
				bool broken = false;
				
				for (int iteration = 0; iteration < 3001; iteration++)
				{
					if (iteration == numIterations)
					{
						gl_FragColor = vec4(0.0, 0.0, 0.0, 1.0);
						
						return;
					}
					
					if (length(z) >= 4.0)
					{
						break;
					}
					
					z = vec2(z.x * z.x - z.y * z.y, 2.0 * z.x * z.y) + usableC;
					
					brightness += exp(-length(z));
				}
				
				gl_FragColor = vec4(brightness / brightnessScale * color, 1.0);
			}
		`,julia:`
			precision highp float;
			
			varying vec2 uv;
			
			uniform vec2 worldCenter;
			uniform vec2 worldSize;
			
			uniform vec2 c;
			uniform int numIterations;
			uniform float brightnessScale;
			
			
			
			void main(void)
			{
				vec2 z = uv * worldSize * 0.5 + worldCenter - vec2(0.75, 0.0);
				
				vec3 color = normalize(
					vec3(
						abs(z.x + z.y) / 2.0,
						abs(z.x) / 2.0,
						abs(z.y) / 2.0
					)
					+ .1 / length(z) * vec3(1.0)
				);
				
				float brightness = exp(-length(z));
				
				
				
				for (int iteration = 0; iteration < 3001; iteration++)
				{
					if (iteration == numIterations)
					{
						gl_FragColor = vec4(0.0, 0.0, 0.0, 1.0);
						return;
					}
					
					if (length(z) >= 4.0)
					{
						break;
					}
					
					z = vec2(z.x * z.x - z.y * z.y, 2.0 * z.x * z.y) + c;
					
					brightness += exp(-length(z));
				}
				
				
				gl_FragColor = vec4(brightness / brightnessScale * color, 1.0);
			}
		`,juliaToMandelbrot:`
			precision highp float;
			
			varying vec2 uv;
			
			uniform vec2 worldCenter;
			uniform vec2 worldSize;
			
			uniform vec2 c;
			uniform int numIterations;
			uniform float brightnessScale;
			uniform float juliaProportion;
			
			
			
			void main(void)
			{
				vec2 z = uv * worldSize * 0.5 + worldCenter - vec2(0.75, 0.0);
				
				vec3 color = normalize(
					vec3(
						abs(z.x + z.y) / 2.0,
						abs(z.x) / 2.0,
						abs(z.y) / 2.0
					)
					+ .1 / length(z) * vec3(1.0)
				);
				
				float brightness = exp(-length(z));

				vec2 usableC = mix(z, c, juliaProportion);
				
				
				
				for (int iteration = 0; iteration < 3001; iteration++)
				{
					if (iteration == numIterations)
					{
						gl_FragColor = vec4(0.0, 0.0, 0.0, 1.0);
						return;
					}
					
					if (length(z) >= 4.0)
					{
						break;
					}
					
					z = vec2(z.x * z.x - z.y * z.y, 2.0 * z.x * z.y) + usableC;
					
					brightness += exp(-length(z));
				}
				
				
				gl_FragColor = vec4(brightness / brightnessScale * color, 1.0);
			}
		`},uniforms:{mandelbrot:{worldCenter:[0,0],worldSize:[4,4],numIterations:this.numIterations,brightnessScale:10},juliaPicker:{worldCenter:[0,0],worldSize:[4,4],c:this.c,numIterations:this.numIterations,brightnessScale:10,juliaRadius:1,crosshairSize:.002},julia:{worldCenter:[0,0],worldSize:[4,4],c:this.c,numIterations:this.numIterations,brightnessScale:10},juliaToMandelbrot:{worldCenter:[0,0],worldSize:[4,4],c:this.c,numIterations:this.numIterations,brightnessScale:10,juliaProportion:1}},canvasWidth:this.resolution,worldWidth:4,worldCenterX:0,worldCenterY:0,minWorldX:-2,maxWorldX:2,minWorldY:-2,maxWorldY:2,minWorldWidth:1e-5,minWorldHeight:1e-5,useResetButton:!0,resetButtonIconPath:"/graphics/general-icons/reset.png",onResizeCanvas:()=>this.needNewFrame=!0,interactionOptions:{useForPanAndZoom:!0,onPanAndZoom:()=>this.needNewFrame=!0,callbacks:{mousemove:this.onMousemove.bind(this),mousedown:this.onMousedown.bind(this),touchstart:this.onTouchstart.bind(this),touchmove:this.onTouchmove.bind(this),touchend:this.onTouchend.bind(this)}},fullscreenOptions:{onSwitch:this.switchFullscreen.bind(this),beforeSwitch:this.beforeSwitchFullscreen.bind(this),fillScreen:!0,useFullscreenButton:!0,enterFullscreenButtonIconPath:"/graphics/general-icons/enter-fullscreen.png",exitFullscreenButtonIconPath:"/graphics/general-icons/exit-fullscreen.png"},verbose:window.DEBUG},e=(this.wilson=new WilsonGPU(canvas,i),this.wilson.useShader("mandelbrot"),this.createHiddenCanvas());this.wilsonHidden=new WilsonGPU(e,{...i,canvasWidth:this.resolutionHidden}),this.wilsonHidden.useShader("mandelbrot"),this.needNewFrame=!0,this.resume()}async advanceJuliaMode(){if("mandelbrot"===this.juliaMode)this.juliaMode="juliaPicker",this.wilson.setUniforms({juliaRadius:bubbleRadius},"juliaPicker"),this.wilsonHidden.setUniforms({juliaRadius:bubbleRadius},"juliaPicker"),this.c=[1e3,1e3];else if("julia"===this.juliaMode){this.juliaMode="juliaToMandelbrot",this.wilson.useShader(this.juliaMode),this.wilsonHidden.useShader(this.juliaMode),this.wilson.setUniforms({juliaProportion:1,c:this.c}),this.wilsonHidden.setUniforms({juliaProportion:1,c:this.c});const e=this.wilson.worldWidth,s=this.wilson.worldHeight;var i=-Math.min(Math.log2(e/4),Math.log2(s/4));await animate(t=>{this.wilson.resizeWorld({width:e*(1-t)+4*t,height:s*(1-t)+4*t}),this.needNewFrame=!0},1<i?500:0<i?200:0,"easeInOutCubic"),0<i&&await sleep(100),await animate(t=>{this.wilson.setUniforms({juliaProportion:1-t}),this.wilsonHidden.setUniforms({juliaProportion:1-t}),this.wilson.resizeWorld({minX:.75*(1-t)-2,maxX:2+.75*(1-t)}),this.needNewFrame=!0},500,"easeInOutQuad"),this.juliaMode="mandelbrot"}else this.juliaMode="julia",this.ignoreBrightnessCalculation=!0,animate(t=>{this.wilson.setUniforms({juliaRadius:(1-t)*bubbleRadius}),this.wilsonHidden.setUniforms({juliaRadius:(1-t)*bubbleRadius}),this.needNewFrame=!0},600,"cubicBezier(0.2, 1, 0.2, 1)"),await sleep(500),this.ignoreBrightnessCalculation=!1,await animate(t=>{this.wilson.resizeWorld({minX:.75*t-2,maxX:2+.75*t}),this.needNewFrame=!0},300,"easeInOutQuad"),this.c[0]-=.75;this.wilson.useShader(this.juliaMode),this.wilsonHidden.useShader(this.juliaMode),"juliaPicker"===this.juliaMode&&currentlyTouchDevice?this.wilson.useInteractionForPanAndZoom=!1:this.wilson.useInteractionForPanAndZoom=!0,this.switchJuliaModeButton&&(this.switchJuliaModeButton.disabled="juliaPicker"===this.juliaMode),this.needNewFrame=!0}onMousemove({x,y}){"juliaPicker"===this.juliaMode&&(this.c=[x,y],this.wilson.setUniforms({crosshairSize:0}),this.wilsonHidden.setUniforms({crosshairSize:0}),this.needNewFrame=!0,this.needNewFrame=!0)}onMousedown(){"juliaPicker"===this.juliaMode&&this.advanceJuliaMode()}onTouchstart({x,y,event}){var i;event.preventDefault(),"juliaPicker"===this.juliaMode&&(i=.15*Math.min(this.wilson.worldHeight,this.wilson.worldWidth),this.c=[x,y+i],currentlyTouchDevice&&animate(t=>{this.wilson.setUniforms({crosshairSize:.02*t}),this.wilsonHidden.setUniforms({crosshairSize:.02*t}),this.needNewFrame=!0},100,"easeOutQuad"),this.needNewFrame=!0)}onTouchmove({x,y,event}){var i;event.preventDefault(),"juliaPicker"===this.juliaMode&&(i=.15*Math.min(this.wilson.worldHeight,this.wilson.worldWidth),this.c=[x,y+i],this.needNewFrame=!0)}onTouchend(){"juliaPicker"===this.juliaMode&&(this.advanceJuliaMode(),animate(t=>{this.wilson.setUniforms({crosshairSize:.02*(1-t)}),this.wilsonHidden.setUniforms({crosshairSize:.02*(1-t)}),this.needNewFrame=!0},100,"easeOutQuad"))}updateBrightnessScale(zoomLevel){this.wilsonHidden.setUniforms({worldSize:[this.wilson.worldWidth,this.wilson.worldHeight],worldCenter:[this.wilson.worldCenterX,this.wilson.worldCenterY],numIterations:this.numIterations,brightnessScale:20+zoomLevel}),"mandelbrot"!==this.juliaMode&&this.wilsonHidden.setUniforms({c:this.c}),this.wilsonHidden.drawFrame();var i=this.wilsonHidden.readPixels(),e=new Array(this.resolutionHidden*this.resolutionHidden);for(let s=0;s<this.resolutionHidden*this.resolutionHidden;s++)e[s]=i[4*s]+i[4*s+1]+i[4*s+2];e.sort((a,b)=>a-b);var t=Math.max((e[Math.floor(this.resolutionHidden*this.resolutionHidden*.96)]+e[Math.floor(this.resolutionHidden*this.resolutionHidden*.98)])/25,4);this.pastBrightnessScales.push(t)}drawFrame(){var i=3-Math.log2(this.wilson.worldWidth);this.numIterations=Math.ceil(200+50*i),this.ignoreBrightnessCalculation||this.updateBrightnessScale(i),10<this.pastBrightnessScales.length&&this.pastBrightnessScales.shift();let e=0;for(let t=0;t<this.pastBrightnessScales.length;t++)e+=this.pastBrightnessScales[t];e=Math.max(e/this.pastBrightnessScales.length,.5),this.wilson.setUniforms({worldSize:[this.wilson.worldWidth,this.wilson.worldHeight],worldCenter:[this.wilson.worldCenterX,this.wilson.worldCenterY],numIterations:this.numIterations,brightnessScale:e}),"mandelbrot"!==this.juliaMode&&this.wilson.setUniforms({c:this.c}),this.wilson.drawFrame()}switchFullscreen(){this.resume()}async beforeSwitchFullscreen(){this.animationPaused=!0,await sleep(33)}}export{JuliaSetExplorer};