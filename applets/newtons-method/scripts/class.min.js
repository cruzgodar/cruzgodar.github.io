import{hexToRgb,rgbToHex}from"../../../scripts/applets/applet.min.js";import anime from"/scripts/anime.min.js";import{AnimationFrameApplet}from"/scripts/applets/animationFrameApplet.min.js";import{siteSettings}from"/scripts/src/settings.min.js";import{WilsonGPU}from"/scripts/wilson.min.js";class NewtonsMethod extends AnimationFrameApplet{wilsonHidden;rootSetterElement;rootAInput;rootBInput;colorSetterElement;colors={root0:[216/255,1/255,42/255],root1:[1,139/255,56/255],root2:[249/255,239/255,20/255],root3:[27/255,181/255,61/255],root4:[0,86/255,195/255],root5:[154/255,82/255,164/255],root6:[32/255,32/255,32/255],root7:[155/255,92/255,15/255]};lastActiveRoot="root0";numRoots=3;numIterations=100;secantProportion=0;pastBrightnessScales=[];resolution=500;resolutionHidden=50;constructor({canvas,rootSetterElement,rootAInput,rootBInput,colorSetterElement}){super(canvas),this.rootSetterElement=rootSetterElement,this.rootAInput=rootAInput,this.rootBInput=rootBInput,this.colorSetterElement=colorSetterElement;var o=this.createHiddenCanvas(),t={shader:`
			precision highp float;
			
			varying vec2 uv;
			
			uniform vec2 worldCenter;
			uniform vec2 worldSize;
			
			uniform int numRoots;
			
			uniform vec2 root0;
			uniform vec2 root1;
			uniform vec2 root2;
			uniform vec2 root3;
			uniform vec2 root4;
			uniform vec2 root5;
			uniform vec2 root6;
			uniform vec2 root7;
			
			uniform vec3 color0;
			uniform vec3 color1;
			uniform vec3 color2;
			uniform vec3 color3;
			uniform vec3 color4;
			uniform vec3 color5;
			uniform vec3 color6;
			uniform vec3 color7;
			
			uniform vec2 a;
			uniform vec2 c;
			
			uniform float brightnessScale;

			uniform float secantProportion;
			
			const float derivativePrecision = 6.0;
			
			const float threshhold = .05;
			
			
			
			//Returns z1 * z2.
			vec2 cmul(vec2 z1, vec2 z2)
			{
				return vec2(z1.x * z2.x - z1.y * z2.y, z1.x * z2.y + z1.y * z2.x);
			}
			
			
			
			//Returns 1/z.
			vec2 cinv(vec2 z)
			{
				float magnitude = z.x*z.x + z.y*z.y;
				
				return vec2(z.x / magnitude, -z.y / magnitude);
			}
			
			
			
			//Returns f(z) for a polynomial f with given roots.
			vec2 cpoly(vec2 z)
			{
				vec2 result = vec2(1.0, 0.0);

				if (numRoots >= 1)
				{
					result = cmul(result, z - root0);
				}

				if (numRoots >= 2)
				{
					result = cmul(result, z - root1);
				}

				if (numRoots >= 3)
				{
					result = cmul(result, z - root2);
				}

				if (numRoots >= 4)
				{
					result = cmul(result, z - root3);
				}

				if (numRoots >= 5)
				{
					result = cmul(result, z - root4);
				}

				if (numRoots >= 6)
				{
					result = cmul(result, z - root5);
				}

				if (numRoots >= 7)
				{
					result = cmul(result, z - root6);
				}

				if (numRoots >= 8)
				{
					result = cmul(result, z - root7);
				}

				return result;
			}
			
			
			
			//Approximates f'(z) for a polynomial f with given roots.
			vec2 cderiv(vec2 z)
			{
				return derivativePrecision * (cpoly(z + vec2(1.0 / (2.0*derivativePrecision), 0.0)) - cpoly(z - vec2(1.0 / (2.0*derivativePrecision), 0.0)));
			}



			void computeColor(
				vec2 lastZ,
				vec2 root,
				vec3 color,
				float d0,
				int iteration
			) {
				float d1 = length(lastZ - root);
				
				float brightnessAdjust = (log(threshhold) - log(d0)) / (log(d1) - log(d0));
				
				float brightness = 1.0 - (float(iteration) - brightnessAdjust) / brightnessScale;
				
				gl_FragColor = vec4(color * brightness, 1.0);
			}
			
			
			
			void main(void)
			{
				vec2 z = uv * worldSize * 0.5 + worldCenter;
				vec2 lastZ = vec2(0.0, 0.0);
				
				gl_FragColor = vec4(0.0, 0.0, 0.0, 1.0);
				
				
				
				for (int iteration = 0; iteration < 100; iteration++)
				{
					vec2 temp = mix(
						cmul(cmul(cpoly(z), cinv(cderiv(z))), a) + c * 0.1,
						cmul(cmul(cpoly(z), cmul(z - lastZ, cinv(cpoly(z) - cpoly(lastZ)))), a) + c * 0.1,
						secantProportion
					);
					
					lastZ = z;
					
					z -= temp;

					if (numRoots >= 1)
					{
						float d0 = length(z - root0);

						if (d0 < threshhold)
						{
							computeColor(lastZ, root0, color0, d0, iteration);
							return;
						}
					}

					if (numRoots >= 2)
					{
						float d0 = length(z - root1);

						if (d0 < threshhold)
						{
							computeColor(lastZ, root1, color1, d0, iteration);
							return;
						}
					}

					if (numRoots >= 3)
					{
						float d0 = length(z - root2);

						if (d0 < threshhold)
						{
							computeColor(lastZ, root2, color2, d0, iteration);
							return;
						}
					}

					if (numRoots >= 4)
					{
						float d0 = length(z - root3);

						if (d0 < threshhold)
						{
							computeColor(lastZ, root3, color3, d0, iteration);
							return;
						}
					}

					if (numRoots >= 5)
					{
						float d0 = length(z - root4);

						if (d0 < threshhold)
						{
							computeColor(lastZ, root4, color4, d0, iteration);
							return;
						}
					}

					if (numRoots >= 6)
					{
						float d0 = length(z - root5);

						if (d0 < threshhold)
						{
							computeColor(lastZ, root5, color5, d0, iteration);
							return;
						}
					}

					if (numRoots >= 7)
					{
						float d0 = length(z - root6);

						if (d0 < threshhold)
						{
							computeColor(lastZ, root6, color6, d0, iteration);
							return;
						}
					}

					if (numRoots >= 8)
					{
						float d0 = length(z - root7);

						if (d0 < threshhold)
						{
							computeColor(lastZ, root7, color7, d0, iteration);
							return;
						}
					}
				}
			}
		`,uniforms:{worldCenter:[0,0],worldSize:[2,2],numRoots:this.numRoots,root0:[0,0],root1:[0,0],root2:[0,0],root3:[0,0],root4:[0,0],root5:[0,0],root6:[0,0],root7:[0,0],color0:this.colors.root0,color1:this.colors.root1,color2:this.colors.root2,color3:this.colors.root3,color4:this.colors.root4,color5:this.colors.root5,color6:this.colors.root6,color7:this.colors.root7,a:[1,0],c:[0,0],brightnessScale:12.75,secantProportion:this.secantProportion},canvasWidth:this.resolution,worldWidth:4,worldHeight:4,worldCenterX:0,worldCenterY:0,minWorldWidth:1e-5,maxWorldWidth:100,minWorldHeight:1e-5,maxWorldHeight:100,onResizeCanvas:this.drawFrame.bind(this),reduceMotion:siteSettings.reduceMotion,interactionOptions:{useForPanAndZoom:!0,onPanAndZoom:this.drawFrame.bind(this)},draggableOptions:{draggables:{a:[1,0],c:[0,0],root0:[0,0],root1:[0,0],root2:[0,0],root3:[0,0],root4:[0,0],root5:[0,0],root6:[0,0],root7:[0,0]},callbacks:{ondrag:this.onDragDraggable.bind(this),onrelease:this.onReleaseDraggable.bind(this)}},fullscreenOptions:{fillScreen:!0,useFullscreenButton:!0,enterFullscreenButtonIconPath:"/graphics/general-icons/enter-fullscreen.png",exitFullscreenButtonIconPath:"/graphics/general-icons/exit-fullscreen.png"}};this.wilson=new WilsonGPU(canvas,t),this.wilsonHidden=new WilsonGPU(o,{...t,canvasWidth:this.resolutionHidden});for(let s=3;s<8;s++)this.wilson.draggables["root"+s].element.style.display="none";this.spreadRoots(!0),this.resume()}switchMethod(instant){const o={t:this.secantProportion};var t=0===this.secantProportion?1:0;anime({targets:o,t:t,duration:instant?10:1e3,easing:"easeInOutQuad",update:()=>{this.secantProportion=o.t,this.wilson.setUniforms({secantProportion:this.secantProportion}),this.wilsonHidden.setUniforms({secantProportion:this.secantProportion}),this.needNewFrame=!0}})}addRoot(){var o,t;8!==this.numRoots&&(o=3*Math.random()-1.5,t=3*Math.random()-1.5,this.wilson.setDraggablePosition({id:"root"+this.numRoots,location:[o,t]}),this.wilson.draggables["root"+this.numRoots].element.style.display="block",this.wilson.setUniforms({["root"+this.numRoots]:this.wilson.draggables["root"+this.numRoots].location}),this.wilsonHidden.setUniforms({["root"+this.numRoots]:this.wilson.draggables["root"+this.numRoots].location}),this.numRoots++,this.wilson.setUniforms({numRoots:this.numRoots}),this.wilsonHidden.setUniforms({numRoots:this.numRoots}),this.needNewFrame=!0)}removeRoot(){1!==this.numRoots&&(this.numRoots--,this.wilson.draggables["root"+this.numRoots].element.style.display="none",this.wilson.setUniforms({numRoots:this.numRoots}),this.wilsonHidden.setUniforms({numRoots:this.numRoots}),this.needNewFrame=!0)}spreadRoots(noAnimation=!1,randomize=!1){const e=Object.fromEntries(Object.entries(this.wilson.draggables).map(([id,draggable])=>[id,draggable.location])),i={};for(let t=0;t<8;t++){var o=1+.75*randomize*Math.random();i["root"+t]=[o*Math.cos(2*Math.PI*t/this.numRoots),o*Math.sin(2*Math.PI*t/this.numRoots)]}const r={t:0};anime({targets:r,t:1,duration:noAnimation?10:1e3,easing:"easeInOutQuad",update:()=>{for(const s of Object.keys(i)){var o=e[s],t=i[s],o=[(1-r.t)*o[0]+r.t*t[0],(1-r.t)*o[1]+r.t*t[1]];this.wilson.setDraggablePosition({id:s,location:o}),this.wilson.setUniforms({[s]:o}),this.wilsonHidden.setUniforms({[s]:o})}this.needNewFrame=!0},complete:()=>{for(const o of Object.keys(i))this.wilson.setDraggablePosition({id:o,location:i[o]}),this.wilson.setUniforms({[o]:i[o]}),this.wilsonHidden.setUniforms({[o]:i[o]});this.needNewFrame=!0}})}setRoot(x,y){this.wilson.setDraggablePosition({id:this.lastActiveRoot,location:[x,y]}),this.wilson.setUniforms({[this.lastActiveRoot]:[x,y]}),this.wilsonHidden.setUniforms({[this.lastActiveRoot]:[x,y]}),this.needNewFrame=!0}setColor(hex){if(this.lastActiveRoot in this.colors){const i=hexToRgb(hex);var o=i.r/255,t=i.g/255,s=i.b/255,e=(i.r=this.colors[this.lastActiveRoot][0],i.g=this.colors[this.lastActiveRoot][1],i.b=this.colors[this.lastActiveRoot][2],this.lastActiveRoot.slice(4));const r="color"+e;anime({targets:i,r:o,g:t,b:s,easing:"easeInOutQuad",duration:250,update:()=>{this.colors[this.lastActiveRoot][0]=i.r,this.colors[this.lastActiveRoot][1]=i.g,this.colors[this.lastActiveRoot][2]=i.b,this.wilson.setUniforms({[r]:this.colors[this.lastActiveRoot]}),this.wilsonHidden.setUniforms({[r]:this.colors[this.lastActiveRoot]}),this.needNewFrame=!0}})}}onDragDraggable({id,x,y}){this.wilson.setUniforms({[id]:[x,y]}),this.wilsonHidden.setUniforms({[id]:[x,y]}),this.needNewFrame=!0}onReleaseDraggable({id}){var o;this.lastActiveRoot=id,this.rootSetterElement&&this.colorSetterElement&&(this.rootAInput.setValue(Math.round(1e3*this.wilson.draggables[this.lastActiveRoot].location[0])/1e3,!1),this.rootBInput.setValue(Math.round(1e3*this.wilson.draggables[this.lastActiveRoot].location[1])/1e3,!1),this.lastActiveRoot in this.colors)&&(o=this.colors[this.lastActiveRoot],this.colorSetterElement.firstElementChild.value=rgbToHex(255*o[0],255*o[1],255*o[2]))}drawFrame(){this.wilsonHidden.setUniforms({worldSize:[this.wilson.worldWidth,this.wilson.worldHeight],worldCenter:[this.wilson.worldCenterX,this.wilson.worldCenterY]}),this.wilsonHidden.drawFrame();var o=this.wilsonHidden.readPixels(),t=new Array(this.resolutionHidden*this.resolutionHidden);for(let i=0;i<this.resolutionHidden*this.resolutionHidden;i++)t[i]=Math.max(Math.max(o[4*i],o[4*i+1]),o[4*i+2]);t.sort((a,b)=>a-b);let s=Math.min(7e3/(t[Math.floor(this.resolutionHidden*this.resolutionHidden*.96)]+t[Math.floor(this.resolutionHidden*this.resolutionHidden*.98)]),30);this.pastBrightnessScales.push(s);var e=this.pastBrightnessScales.length;10<e&&this.pastBrightnessScales.shift();for(let r=s=0;r<this.pastBrightnessScales.length;r++)s+=this.pastBrightnessScales[r];s=Math.max(s/e,.5),this.wilson.setUniforms({worldSize:[this.wilson.worldWidth,this.wilson.worldHeight],worldCenter:[this.wilson.worldCenterX,this.wilson.worldCenterY],brightnessScale:s}),this.wilson.drawFrame()}}export{NewtonsMethod};