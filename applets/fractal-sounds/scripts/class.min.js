import{getGlslBundle,loadGlsl}from"../../../scripts/src/complexGlsl.min.js";import{AnimationFrameApplet}from"/scripts/applets/animationFrameApplet.min.js";import{tempShader}from"/scripts/applets/applet.min.js";import{changeOpacity}from"/scripts/src/animation.min.js";import{convertColor}from"/scripts/src/browser.min.js";import{animate,sleep}from"/scripts/src/utils.min.js";import{WilsonCPU,WilsonGPU}from"/scripts/wilson.min.js";class FractalSounds extends AnimationFrameApplet{loadPromise;hasRun=!1;glslCode;wilsonJulia;aspectRatio=1;defaultWorldSize=4;zoomLevel=0;resolution=500;needToClear=!1;fixedPointX=0;fixedPointY=0;moved=0;lastX=0;lastY=0;audioNodes=[];constructor({canvas,lineDrawerCanvas}){super(canvas);var t={shader:tempShader,canvasWidth:this.resolution,useResetButton:!0,resetButtonIconPath:"/graphics/general-icons/reset.png",onReset:()=>this.wilson.reset(),fullscreenOptions:{onSwitch:this.onSwitchFullscreen.bind(this),beforeSwitch:this.beforeSwitchFullscreen.bind(this),fillScreen:!0,useFullscreenButton:!0,enterFullscreenButtonIconPath:"/graphics/general-icons/enter-fullscreen.png",exitFullscreenButtonIconPath:"/graphics/general-icons/exit-fullscreen.png"},verbose:window.DEBUG},t=(this.wilsonJulia=new WilsonGPU(canvas,t),{canvasWidth:1500,worldWidth:4,minWorldX:-3,maxWorldX:3,minWorldY:-3,maxWorldY:3,onResizeCanvas:()=>this.needNewFrame=!0,interactionOptions:{useForPanAndZoom:!0,onPanAndZoom:()=>this.needNewFrame=!0,callbacks:{mousemove:this.onHoverCanvas.bind(this),mousedown:this.onGrabCanvas.bind(this),touchstart:this.onGrabCanvas.bind(this),mousedrag:this.onDragCanvas.bind(this),touchmove:this.onDragCanvas.bind(this),mouseup:this.onReleaseCanvas.bind(this),touchend:this.onReleaseCanvas.bind(this),wheel:this.onWheelCanvas.bind(this)}},fullscreenOptions:{fillScreen:!0,animate:!1,closeWithEscape:!1},verbose:window.DEBUG});this.wilson=new WilsonCPU(lineDrawerCanvas,t),this.wilsonForFullscreen=this.wilsonJulia,this.wilson.ctx.lineWidth=40,this.loadPromise=loadGlsl()}getShader({oldGlslCode}){var t=oldGlslCode?`codeInterpolation * (${this.glslCode}) + (1.0 - codeInterpolation) * (${oldGlslCode})`:this.glslCode;return`
			precision highp float;

			varying vec2 uv;

			uniform vec2 worldSize;
			uniform vec2 worldCenter;

			const int numIterations = 200;

			const int numHarmonics = 8;
			const float hueMultiplier = 100.0;

			uniform float codeInterpolation;



			${getGlslBundle(this.glslCode+" "+(oldGlslCode??""))}



			vec3 hsvToRgb(vec3 c)
			{
				vec4 K = vec4(1.0, 2.0 / 3.0, 1.0 / 3.0, 3.0);
				vec3 p = abs(fract(c.xxx + K.xyz) * 6.0 - K.www);
				return c.z * mix(K.xxx, clamp(p - K.xxx, 0.0, 1.0), c.y);
			}



			void main(void)
			{
				vec2 z = uv * worldSize * 0.5 + worldCenter;

				float brightness = exp(-max(length(z), .5));

				vec2 c = z;

				gl_FragColor = vec4(0.0, 0.0, 0.0, 1.0);



				vec2 lastZ[numHarmonics];

				for (int i = 0; i < numHarmonics; i++)
				{
					lastZ[i] = vec2(0.0, 0.0);
				}

				float hitCounts[numHarmonics];

				for (int i = 0; i < numHarmonics; i++)
				{
					hitCounts[i] = 0.0;
				}

				float totalDisplacement = 0.0;



				for (int iteration = 0; iteration < 200; iteration++)
				{
					if (length(z) >= 10.0)
					{
						gl_FragColor = vec4(0.0, 0.0, 0.0, 1.0);
						return;
					}

					for (int i = numHarmonics - 1; i >= 1; i--)
					{
						lastZ[i] = lastZ[i - 1];
					}
					lastZ[0] = z;

					z = ${t};



					brightness += exp(-max(length(z), .5));
					totalDisplacement += length(z - lastZ[0]);

					for (int k = 0; k < numHarmonics; k++)
					{
						hitCounts[k] += step(0.001, exp(-hueMultiplier * length(z - lastZ[k])));
					}
				}

				vec2 hueDir = vec2(0.0, 0.0);
				for (int k = 0; k < numHarmonics; k++)
				{
					float angle = 6.283185 * float(k) / float(numHarmonics);
					hueDir += hitCounts[k] / float(k + 1) * vec2(cos(angle), sin(angle));
				}
				float hue = atan(hueDir.y, hueDir.x) / 6.283185;

				float maxHitCount = 0.0;
				for (int k = 0; k < numHarmonics; k++)
				{
					maxHitCount = max(maxHitCount, hitCounts[k]);
				}

				float activity = 1.0 - exp(-totalDisplacement / float(numIterations));

				float saturation = clamp(
					pow(maxHitCount / float(numIterations), 0.5)
						* activity,
					0.0,
					1.0
				);

				float value = brightness * 0.013 * (0.05 + 0.95 * activity);

				gl_FragColor = vec4(
					hsvToRgb(vec3(hue, saturation, value)), 1.0
				);
			}
		`}async animateResetWorldCoordinates(){const e=this.wilson.worldWidth,i=this.wilson.worldHeight,s=this.wilson.worldCenterX,o=this.wilson.worldCenterY;var n=Math.abs(Math.min(Math.log2(e/this.defaultWorldSize),Math.log2(i/this.defaultWorldSize)));await animate(t=>{this.wilson.resizeWorld({width:e*(1-t)+this.defaultWorldSize*t,height:i*(1-t)+this.defaultWorldSize*t,centerX:s*(1-t),centerY:o*(1-t)}),this.needNewFrame=!0},1<n?500:0<n?200:0,"easeInOutCubic"),0<n&&await sleep(100)}async interpolateBetweenRuns(glslCode){await this.animateResetWorldCoordinates();var t=this.glslCode,t=(this.glslCode=glslCode,this.getShader({oldGlslCode:t}));this.wilsonJulia.loadShader({shader:t,uniforms:{worldSize:[this.wilson.worldWidth,this.wilson.worldHeight],worldCenter:[this.wilson.worldCenterX,this.wilson.worldCenterY],codeInterpolation:0}}),await animate(t=>{this.wilsonJulia.setUniforms({codeInterpolation:t}),this.needNewFrame=!0},750,"easeInOutQuad")}async run({glslCode,jsCode,resolution,animate=!0}){await this.loadPromise,this.currentFractalFunction=jsCode,this.resolution=resolution,this.hasRun&&animate?await this.interpolateBetweenRuns(glslCode):this.glslCode=glslCode;var t=this.getShader({});this.wilsonJulia.loadShader({shader:t,uniforms:{worldSize:[this.wilson.worldWidth,this.wilson.worldHeight],worldCenter:[this.wilson.worldCenterX,this.wilson.worldCenterY]}}),this.wilson.resizeWorld({width:4,height:4,centerX:0,centerY:0}),this.wilsonJulia.resizeCanvas({width:this.resolution}),this.resume(),this.hasRun=!0}onHoverCanvas({x,y}){this.showOrbit(x,y),this.moved=0}onGrabCanvas({x,y,event}){this.wilson.canvas.style.opacity=1,"touchstart"===event.type?(this.numTouches=event.touches.length,1===this.numTouches?this.showOrbit(x,y):changeOpacity({element:this.wilson.canvas,opacity:0})):(this.moved=0,this.showOrbit(x,y))}onDragCanvas({x,y}){this.wilsonJulia.showResetButton(),this.moved++,10<=this.moved&&changeOpacity({element:this.wilson.canvas,opacity:0}),this.showOrbit(x,y)}onReleaseCanvas({x,y,event}){this.moved<10&&(this.playSound(x,y),setTimeout(()=>this.numTouches=0,50)),"touchend"===event.type&&setTimeout(()=>{changeOpacity({element:this.wilson.canvas,opacity:0})},150),this.moved=0}onWheelCanvas({x,y}){this.wilsonJulia.showResetButton(),this.showOrbit(x,y)}showOrbit(x0,y0){this.wilson.ctx.lineWidth=2,this.wilson.canvas.style.opacity=1,this.wilson.ctx.strokeStyle=convertColor(255,255,255),this.wilson.ctx.clearRect(0,0,this.wilson.canvasWidth,this.wilson.canvasHeight),this.wilson.ctx.beginPath();const t=this.wilson.interpolateWorldToCanvas([x0,y0]);this.wilson.ctx.moveTo(t[1],t[0]);var e,i,s=x0,o=y0;let n=this.currentFractalFunction(x0,y0,s,o);for(let a=0;a<300;a++){if(10<Math.abs(n[0])||10<Math.abs(n[1]))return;e=n[0],i=n[1],n=this.currentFractalFunction(e,i,s,o);const t=this.wilson.interpolateWorldToCanvas([e,i]);this.wilson.ctx.lineTo(t[1],t[0])}this.wilson.ctx.stroke()}playSound(x0,y0){const e=new AudioContext;var t=44100,i=Math.floor(3675);let s=x0,o=y0;var n=x0,a=y0;let l=this.currentFractalFunction(s,o,n,a),r=(s=0,o=0);var h=new Array(i),c=new Array(i),t=e.createBuffer(2,t,44100),d=t.getChannelData(0),u=t.getChannelData(1);for(let p=0;p<i;p++){if(100<Math.abs(l[0])||100<Math.abs(l[1]))return;Math.abs(l[0])>r&&(r=Math.abs(l[0])),Math.abs(l[1])>r&&(r=Math.abs(l[1])),h[p]=s,c[p]=o,s=l[0],o=l[1],l=this.currentFractalFunction(s,o,n,a)}for(let f=0;f<i;f++)h[f]/=r,c[f]/=r;for(let C=0;C<i-1;C++)for(let t=0;t<12;t++){var m=.5+.5*Math.sin(Math.PI*t/12-Math.PI/2);d[12*C+t]=(1-m)*(h[C]/2)+m*(h[C+1]/2),u[12*C+t]=(1-m)*(c[C]/2)+m*(c[C+1]/2)}var w=e.createBufferSource(),t=(w.buffer=t,e.createGain());w.connect(t),t.connect(e.destination);const v=[e,w,t];this.audioNodes.push(v),w.onended=()=>{var t=this.audioNodes.indexOf(v);-1!==t&&this.audioNodes.splice(t,1),e.close()},w.start(0),t.gain.exponentialRampToValueAtTime(1e-4,e.currentTime+1)}drawFrame(){this.wilsonJulia.setUniforms({worldSize:[this.wilson.worldWidth,this.wilson.worldHeight],worldCenter:[this.wilson.worldCenterX,this.wilson.worldCenterY]}),this.wilsonJulia.drawFrame()}onSwitchFullscreen(isFullscreen){var t,e;isFullscreen?(this.wilson.enterFullscreen(),t=document.querySelectorAll(".WILSON_canvas-container"),e=document.querySelectorAll(".WILSON_button-container"),t[0].appendChild(e[1])):this.wilson.exitFullscreen(),this.resume()}async beforeSwitchFullscreen(){this.pause(),await sleep(33)}destroyAudioNodes(){for(const[t,,e]of this.audioNodes)e.gain.linearRampToValueAtTime(1e-4,t.currentTime+.05),setTimeout(()=>t.close(),50);this.audioNodes=[]}pause(){for(var[t]of this.audioNodes)t.suspend();super.pause()}resume(){super.resume();for(var[t]of this.audioNodes)t.resume()}destroy(){this.destroyAudioNodes(),super.destroy()}downloadHighResFrame(filename,resolution){this.wilsonJulia.downloadHighResFrame(filename,resolution)}}export{FractalSounds};