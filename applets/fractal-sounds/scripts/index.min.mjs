import{FractalSounds}from"./class.min.mjs";import{showPage}from"/scripts/src/load-page.min.mjs";import{$}from"/scripts/src/main.min.mjs";function load(){const applet=new FractalSounds($("#output-canvas"),$("#line-drawer-canvas")),examples=(applet.loadPromise.then(()=>run()),{mandelbrot:["cmul(z, z) + c",(x,y,a,b)=>[x*x-y*y+a,2*x*y+b]],sfx:["cmul(z, dot(z, z)) - cmul(z, c*c)",(x,y,a,b)=>[x*x*x+x*y*y-x*a*a+y*b*b,x*x*y-x*b*b+y*y*y-y*a*a]],"burning-ship":["-vec2(z.x * z.x - z.y * z.y, 2.0 * abs(z.x * z.y)) + c",(x,y,a,b)=>[-(x*x-y*y)+a,-2*Math.abs(x*y)+b]],feather:["cdiv(cmul(cmul(z, z), z), ONE + z*z) + c",(x,y,a,b)=>[a+(x*x*x*x*x+x*x*x*(1-3*y*y)+3*x*x*y*y*y-3*x*y*y-y*y*y*y*y)/(x*x*x*x+2*x*x+y*y*y*y+1),b+y*(3*x*x*x*x-x*x*x*y-x*x*(y*y-3)+3*x*y*y*y-y*y)/(x*x*x*x+2*x*x+y*y*y*y+1)]],duffing:["vec2(z.y, -c.y * z.x + c.x * z.y - z.y * z.y * z.y)",(x,y,a,b)=>[y,-b*x+a*y-y*y*y]],ikeda:["vec2(1.0 + c.x * (z.x * cos(.4 - 6.0 / (1.0 + dot(z, z))) - z.y * sin(.4 - 6.0 / (1.0 + dot(z, z)))), c.y * (z.x * sin(.4 - 6.0 / (1.0 + dot(z, z))) + z.y * cos(.4 - 6.0 / (1.0 + dot(z, z)))))",(x,y,a,b)=>[1+a*(x*Math.cos(.4-6/(1+x*x+y*y))-y*Math.sin(.4-6/(1+x*x+y*y))),b*(x*Math.sin(.4-6/(1+x*x+y*y))+y*Math.cos(.4-6/(1+x*x+y*y)))]]}),fractalSelectorDropdownElement=$("#fractal-selector-dropdown"),resolutionInputElement=(fractalSelectorDropdownElement.addEventListener("input",run),$("#resolution-input")),exposureInputElement=$("#exposure-input"),numIterationsInputElement=$("#num-iterations-input");function run(){var glslCode=examples[fractalSelectorDropdownElement.value][0],jsCode=examples[fractalSelectorDropdownElement.value][1],resolution=parseInt(resolutionInputElement.value||500),exposure=parseFloat(exposureInputElement.value||1),numIterations=parseInt(numIterationsInputElement.value||200);applet.run({glslCode:glslCode,jsCode:jsCode,resolution:resolution,exposure:exposure,numIterations:numIterations})}applet.setInputCaps([resolutionInputElement,numIterationsInputElement],[2e3,500]),resolutionInputElement.addEventListener("input",()=>{applet.resolution=parseInt(resolutionInputElement.value||500),applet.changeAspectRatio(!0)}),exposureInputElement.addEventListener("input",()=>{applet.exposure=parseFloat(exposureInputElement.value||1)}),numIterationsInputElement.addEventListener("input",()=>{applet.numIterations=parseInt(numIterationsInputElement.value||200)}),$("#download-button").addEventListener("click",()=>{applet.wilson.downloadFrame("a-sound-fractal.png")}),showPage()}export{load};