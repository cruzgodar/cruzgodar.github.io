import{Applet,getMinGlslString,getVectorGlsl}from"/scripts/src/applets.min.js";import{Wilson}from"/scripts/wilson.min.js";class VoronoiDiagram extends Applet{lastTimestamp=-1;numPoints=20;metric=2;resolution=1e3;radius;pointRadius;points;colors;constructor({canvas}){super(canvas);var i={renderer:"gpu",shader:`
			precision highp float;
			
			varying vec2 uv;
			
			void main(void)
			{
				gl_FragColor = vec4(0, 0, 0, 1);
			}
		`,canvasWidth:this.resolution,canvasHeight:this.resolution,useFullscreen:!0,useFullscreenButton:!0,enterFullscreenButtonIconPath:"/graphics/general-icons/enter-fullscreen.png",exitFullscreenButtonIconPath:"/graphics/general-icons/exit-fullscreen.png"};this.wilson=new Wilson(this.canvas,i)}run({resolution=500,numPoints=20,metric=2}){this.resolution=resolution,this.numPoints=numPoints,this.metric=metric,this.radius=0,this.wilson.changeCanvasSize(this.resolution,this.resolution),this.generatePoints();var i=1===this.metric?`
					abs(p.x - q.x) + abs(p.y - q.y)
				`:2===this.metric?`
					distance(p, q)
				`:`
					max(abs(p.x - q.x), abs(p.y - q.y))
				`,i=`
			precision highp float;
			
			varying vec2 uv;

			uniform float radius;

			const float pointRadius = 0.01;
			const float blurRatio = 0.5;
			const float boundaryWidth = 0.02;

	${this.points.map((point,index)=>`
			const vec2 point${index} = ${getVectorGlsl(point)};
		`).join("")}

	${this.colors.map((color,index)=>`
			const vec3 color${index} = ${getVectorGlsl(color)};
		`).join("")}
			
			float metricDistance(vec2 p, vec2 q)
			{
				return ${i};
			}

			float getMinDistanceToPoints(vec2 p, out vec3 color)
			{
	${this.points.map((point,index)=>`
			float distance${index+1} = metricDistance(p, point${index});
		`).join("")}

				float minDistance = ${getMinGlslString("distance",this.numPoints)};

	${this.colors.map((color,index)=>`
			if (minDistance == distance${index+1})
			{
				color = color${index};
				return minDistance;
			}
		`).join("")}
			}

			void main(void)
			{
				vec3 color;
				float minDistance = getMinDistanceToPoints(uv, color);

				if (minDistance < pointRadius)
				{
					gl_FragColor = vec4(1, 1, 1, 1);
					return;
				}

				if (minDistance < (1.0 + blurRatio) * pointRadius)
				{
					float t = 1.0 - (minDistance - pointRadius) / (blurRatio * pointRadius);

					gl_FragColor = vec4(t, t, t, 1);
					return;
				}

				if (minDistance < radius)
				{
					gl_FragColor = vec4(color, 1);
					return;
				}

				if (minDistance < radius + boundaryWidth)
				{
					gl_FragColor = vec4(color * 0.5, 1);
					return;
				}

				gl_FragColor = vec4(0, 0, 0, 1);
			}
		`;window.DEBUG&&console.log(i),this.wilson.render.shaderPrograms=[],this.wilson.render.loadNewShader(i),this.wilson.gl.useProgram(this.wilson.render.shaderPrograms[0]),this.wilson.render.initUniforms(["radius"]),this.wilson.gl.uniform1f(this.wilson.uniforms.radius,this.radius),window.requestAnimationFrame(this.drawFrame.bind(this))}generatePoints(){this.points=new Array(this.numPoints);for(let o=0;o<this.numPoints;o++)this.points[o]=[.8*(Math.random()-.5)*this.wilson.worldWidth,.8*(Math.random()-.5)*this.wilson.worldHeight];var t,s=new Array(this.numPoints),i=.1/this.numPoints;for(let n=0;n<this.numPoints;n++){s[n]=[0,0];for(let i=0;i<this.numPoints;i++)i!==n&&(t=(this.points[i][0]-this.points[n][0])**2+(this.points[i][1]-this.points[n][1])**2,s[n][0]+=(this.points[n][0]-this.points[i][0])/t,s[n][1]+=(this.points[n][1]-this.points[i][1])/t)}for(let r=0;r<this.numPoints;r++)this.points[r][0]+=i*s[r][0],this.points[r][1]+=i*s[r][1],this.points[r][0]=Math.min(Math.max(this.points[r][0],-this.wilson.worldWidth/2),this.wilson.worldWidth/2),this.points[r][1]=Math.min(Math.max(this.points[r][1],-this.wilson.worldHeight/2),this.wilson.worldHeight/2);this.colors=new Array(this.numPoints);for(let e=0;e<this.numPoints;e++)this.colors[e]=this.wilson.utils.hsvToRgb(Math.random(),.5+.25*Math.random(),.5+.5*Math.random()),this.colors[e][0]/=255,this.colors[e][1]/=255,this.colors[e][2]/=255}drawFrame(timestamp){var i=timestamp-this.lastTimestamp;-1===this.lastTimestamp?(this.lastTimestamp=timestamp,window.requestAnimationFrame(this.drawFrame.bind(this))):(this.lastTimestamp=timestamp,0==i||(this.wilson.render.drawFrame(),this.radius+=5e-4*i,this.wilson.gl.uniform1f(this.wilson.uniforms.radius,this.radius),this.wilson.render.drawFrame(),this.animationPaused)||window.requestAnimationFrame(this.drawFrame.bind(this)))}}export{VoronoiDiagram};