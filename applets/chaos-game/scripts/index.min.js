"use strict";!async function(){await Site.loadApplet("chaos-game");let e=new ChaosGame($("#output-canvas"));function t(){let t=parseInt(l.value||1e3),n=parseInt(o.value||5);e.run(t,n)}let n=$("#generate-button");n.addEventListener("click",t);let l=$("#resolution-input"),o=$("#num-vertices-input");e.listenToInputElements([l,o],t);let u=$("#download-button");u.addEventListener("click",()=>e.wilson.downloadFrame("a-chaos-game.png")),Page.show()}();