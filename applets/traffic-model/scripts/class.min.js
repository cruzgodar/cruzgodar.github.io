import{AnimationFrameApplet}from"/scripts/applets/animationFrameApplet.min.js";import{WilsonGPU}from"/scripts/wilson.min.js";class AbelianSandpiles extends AnimationFrameApplet{resolution=500;density=.5;computationsPerFrame=1;lastPixelData;constructor({canvas}){super(canvas);var e={shaders:{init:`
			precision highp float;
			precision highp sampler2D;
			
			varying vec2 uv;
			
			uniform sampler2D uTexture;
			
			uniform float density;
			
			float rand(vec2 co)
			{
				co += vec2(${Math.random()}, ${Math.random()});
				return fract(sin(dot(co, vec2(12.9898, 78.233))) * 43758.5453);
			}
			
			void main(void)
			{
				float random = rand(uv);

				if (random < density * 0.5)
				{
					// North
					gl_FragColor = vec4(1.0, 0.0, 0.0, 1.0);
				}

				else if (random < density)
				{
					// East
					gl_FragColor = vec4(0.5, 0.0, 0.0, 1.0);
				}

				else
				{
					// Empty
					gl_FragColor = vec4(0.0, 0.0, 0.0, 1.0);
				}
			}
		`,updateNorth:`
			precision highp float;
			precision highp sampler2D;
			
			varying vec2 uv;
			
			uniform sampler2D uTexture;
			uniform float stepSize;
			
			void main(void)
			{
				vec2 center = (uv + vec2(1.0, 1.0)) * 0.5;

				float here = texture2D(uTexture, center).x;

				if (here == 0.0)
				{
					float south = texture2D(uTexture, mod(center + vec2(0.0, -stepSize), 1.0)).x;

					if (south == 1.0)
					{
						gl_FragColor = vec4(1.0, 0.0, 0.0, 1.0);
					}

					else
					{
						gl_FragColor = vec4(0.0, 0.0, 0.0, 1.0);
					}
				}

				else if (here == 1.0)
				{
					float north = texture2D(uTexture, mod(center + vec2(0.0, stepSize), 1.0)).x;

					if (north == 0.0)
					{
						gl_FragColor = vec4(0.0, 0.0, 0.0, 1.0);
					}

					else
					{
						gl_FragColor = vec4(1.0, 0.0, 0.0, 1.0);
					}
				}

				else
				{
					gl_FragColor = vec4(here, 0.0, 0.0, 1.0);
				}
			}
		`,updateEast:`
			precision highp float;
			precision highp sampler2D;
			
			varying vec2 uv;
			
			uniform sampler2D uTexture;
			uniform float stepSize;
			
			void main(void)
			{
				vec2 center = (uv + vec2(1.0, 1.0)) * 0.5;

				float here = texture2D(uTexture, center).x;

				if (here == 0.0)
				{
					float west = texture2D(uTexture, mod(center + vec2(-stepSize, 0.0), 1.0)).x;

					if (west == 0.5)
					{
						gl_FragColor = vec4(0.5, 0.0, 0.0, 1.0);
					}

					else
					{
						gl_FragColor = vec4(0.0, 0.0, 0.0, 1.0);
					}
				}

				else if (here == 0.5)
				{
					float east = texture2D(uTexture, mod(center + vec2(stepSize, 0.0), 1.0)).x;

					if (east == 0.0)
					{
						gl_FragColor = vec4(0.0, 0.0, 0.0, 1.0);
					}

					else
					{
						gl_FragColor = vec4(0.5, 0.0, 0.0, 1.0);
					}
				}

				else
				{
					gl_FragColor = vec4(here, 0.0, 0.0, 1.0);
				}
			}
		`,draw:`
			precision highp float;
			precision highp sampler2D;
			
			varying vec2 uv;
			
			uniform sampler2D uTexture;
			
			void main(void)
			{
				vec2 center = (uv + vec2(1.0, 1.0)) * 0.5;

				float here = texture2D(uTexture, center).x;

				if (here == 1.0)
				{
					gl_FragColor = vec4(0.5, 0.0, 1.0, 1.0);
				}

				else if (here == 0.5)
				{
					gl_FragColor = vec4(0.0, 0.0, 1.0, 1.0);
				}

				else
				{
					gl_FragColor = vec4(0.0, 0.0, 0.0, 1.0);
				}
			}
		`},uniforms:{init:{density:this.density},updateNorth:{stepSize:1/this.resolution},updateEast:{stepSize:1/this.resolution}},canvasWidth:this.resolution,fullscreenOptions:{onSwitch:this.switchFullscreen.bind(this),beforeSwitch:this.beforeSwitchFullscreen.bind(this),useFullscreenButton:!0,enterFullscreenButtonIconPath:"/graphics/general-icons/enter-fullscreen.png",exitFullscreenButtonIconPath:"/graphics/general-icons/exit-fullscreen.png"}};this.wilson=new WilsonGPU(canvas,e),this.canvas.style.imageRendering="pixelated"}run({resolution=100,density=.5,computationsPerFrame=1}){this.resolution=resolution,this.density=density,this.computationsPerFrame=computationsPerFrame,this.wilson.setUniforms({density:this.density},"init"),this.wilson.setUniforms({stepSize:1/this.resolution},"updateNorth"),this.wilson.setUniforms({stepSize:1/this.resolution},"updateEast"),this.wilson.resizeCanvas({width:this.resolution}),this.wilson.createFramebufferTexturePair({id:"0",textureType:"float"}),this.wilson.createFramebufferTexturePair({id:"1",textureType:"float"}),this.wilson.useShader("init"),this.wilson.useTexture("1"),this.wilson.useFramebuffer("0"),this.wilson.drawFrame(),this.resume()}drawFrame(){for(let e=0;e<this.computationsPerFrame;e++)this.wilson.useShader("updateNorth"),this.wilson.useTexture("0"),this.wilson.useFramebuffer("1"),this.wilson.drawFrame(),this.wilson.useShader("updateEast"),this.wilson.useTexture("1"),this.wilson.useFramebuffer("0"),this.wilson.drawFrame();this.wilson.useShader("draw"),this.wilson.useTexture("0"),this.wilson.useFramebuffer(null),this.wilson.drawFrame(),this.needNewFrame=!0}switchFullscreen(){this.resume()}async beforeSwitchFullscreen(){this.pause(),await new Promise(resolve=>setTimeout(resolve,33))}}export{AbelianSandpiles};