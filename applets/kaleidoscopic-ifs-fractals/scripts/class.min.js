import anime from"/scripts/anime.min.js";import{RaymarchApplet}from"/scripts/applets/raymarchApplet.min.js";import{changeOpacity}from"/scripts/src/animation.min.js";import{aspectRatio}from"/scripts/src/layout.min.js";import{addTemporaryListener}from"/scripts/src/main.min.js";import{Wilson}from"/scripts/wilson.min.js";class KaleidoscopicIFSFractal extends RaymarchApplet{numSierpinskiIterations=24;scale=2;startingCameraPos=[[.7752,1.3176,.5478],[1.1748,1.1675,1.1673],[-1.7346,-.4485,.2596]];startingAngle=[[4.1804,1.8147],[3.9036,2.2196],[.2004,1.6538]];cameraPos=[...this.startingCameraPos[2]];theta=this.startingAngle[2][0];phi=this.startingAngle[2][1];polyhedronIndex=2;lightPos=[[0,0,5],[5,5,5],[0,0,5]];n1=[[-.57735,0,.816496],[1,0,0],[.707107,0,.707107]];n2=[[.288675,-.5,.816496],[0,1,0],[0,.707107,.707107]];n3=[[.288675,.5,.816496],[0,0,1],[-.707107,0,.707107]];n4=[[],[],[0,-.707107,.707107]];numNs=[3,3,4];scaleCenter=[[0,0,1],[.57735,.57735,.57735],[0,0,1]];rotationAngleX1=0;rotationAngleY1=0;rotationAngleZ1=0;rotationAngleX2=0;rotationAngleY2=0;rotationAngleZ2=0;constructor({canvas}){super(canvas);var t={renderer:"gpu",shader:`
			precision highp float;
			
			varying vec2 uv;
			
			uniform float aspectRatioX;
			uniform float aspectRatioY;
			
			uniform vec3 cameraPos;
			uniform vec3 imagePlaneCenterPos;
			uniform vec3 forwardVec;
			uniform vec3 rightVec;
			uniform vec3 upVec;
			
			uniform float focalLength;
			
			uniform vec3 lightPos;
			const float lightBrightness = 2.0;
			
			uniform int imageSize;
			
			
			
			const float clipDistance = 1000.0;
			const int maxMarches = 32;
			const vec3 fogColor = vec3(0.0, 0.0, 0.0);
			const float fogScaling = .2;
			const int maxIterations = 24;
			
			
			
			const vec3 color1 = vec3(1.0, 0.0, 0.0);
			const vec3 color2 = vec3(0.0, 1.0, 0.0);
			const vec3 color3 = vec3(0.0, 0.0, 1.0);
			const vec3 color4 = vec3(1.0, 1.0, 0.0);
			
			
			
			uniform vec3 scaleCenter;
			
			uniform int numNs;
			
			uniform vec3 n1;
			uniform vec3 n2;
			uniform vec3 n3;
			uniform vec3 n4;
			
			
			
			const float scale = 2.0;
			
			
			
			uniform mat3 rotationMatrix1;
			uniform mat3 rotationMatrix2;
			
			
			
			float distanceEstimator(vec3 pos)
			{
				vec3 mutablePos = pos;
				
				//We'll find the closest vertex, scale everything by a factor of 2 centered on that vertex (so that we don't need to recalculate the vertices), and repeat.
				for (int iteration = 0; iteration < maxIterations; iteration++)
				{
					//Fold space over on itself so that we can reference only the top vertex.
					float t1 = dot(mutablePos, n1);
					
					if (t1 < 0.0)
					{
						mutablePos -= 2.0 * t1 * n1;
					}
					
					float t2 = dot(mutablePos, n2);
					
					if (t2 < 0.0)
					{
						mutablePos -= 2.0 * t2 * n2;
					}
					
					float t3 = dot(mutablePos, n3);
					
					if (t3 < 0.0)
					{
						mutablePos -= 2.0 * t3 * n3;
					}
					
					if (numNs >= 4)
					{
						float t4 = dot(mutablePos, n4);
						
						if (t4 < 0.0)
						{
							mutablePos -= 2.0 * t4 * n4;
						}
					}
					
					
					
					mutablePos = rotationMatrix1 * mutablePos;
					
					//Scale the system -- this one takes me a fair bit of thinking to get. What's happening here is that we're stretching from a vertex, but since we never scale the vertices, the four new ones are the four closest to the vertex we scaled from. Now (x, y, z) will get farther and farther away from the origin, but that makes sense -- we're really just zooming in on the tetrahedron.
					mutablePos = scale * mutablePos - (scale - 1.0) * scaleCenter;
					
					mutablePos = rotationMatrix2 * mutablePos;
				}
				
				return length(mutablePos) * pow(1.0/scale, float(maxIterations));
			}
			
			
			
			vec3 getColor(vec3 pos)
			{
				vec3 mutablePos = pos;
				
				vec3 color = vec3(1.0, 1.0, 1.0);
				float colorScale = .5;
				
				
				
				//We'll find the closest vertex, scale everything by a factor of 2 centered on that vertex (so that we don't need to recalculate the vertices), and repeat.
				for (int iteration = 0; iteration < maxIterations; iteration++)
				{
					//Fold space over on itself so that we can reference only the top vertex.
					float t1 = dot(mutablePos, n1);
					
					if (t1 < 0.0)
					{
						mutablePos -= 2.0 * t1 * n1;
						
						color = mix(color, color1, colorScale);
					}
					
					float t2 = dot(mutablePos, n2);
					
					if (t2 < 0.0)
					{
						mutablePos -= 2.0 * t2 * n2;
						
						color = mix(color, color2, colorScale);
					}
					
					float t3 = dot(mutablePos, n3);
					
					if (t3 < 0.0)
					{
						mutablePos -= 2.0 * t3 * n3;
						
						color = mix(color, color3, colorScale);
					}
					
					if (numNs >= 4)
					{
						float t4 = dot(mutablePos, n4);
						
						if (t4 < 0.0)
						{
							mutablePos -= 2.0 * t4 * n4;
							
							color = mix(color, color4, colorScale);
						}
					}
					
					
					
					mutablePos = rotationMatrix1 * mutablePos;
					
					//Scale the system -- this one takes me a fair bit of thinking to get. What's happening here is that we're stretching from a vertex, but since we never scale the vertices, the four new ones are the four closest to the vertex we scaled from. Now (x, y, z) will get farther and farther away from the origin, but that makes sense -- we're really just zooming in on the tetrahedron.
					mutablePos = scale * mutablePos - (scale - 1.0) * scaleCenter;
					
					mutablePos = rotationMatrix2 * mutablePos;
					
					colorScale *= .5;
				}
				
				
				
				return color;
			}
			
			
			
			vec3 getSurfaceNormal(vec3 pos)
			{
				float xStep1 = distanceEstimator(pos + vec3(.000001, 0.0, 0.0));
				float yStep1 = distanceEstimator(pos + vec3(0.0, .000001, 0.0));
				float zStep1 = distanceEstimator(pos + vec3(0.0, 0.0, .000001));
				
				float xStep2 = distanceEstimator(pos - vec3(.000001, 0.0, 0.0));
				float yStep2 = distanceEstimator(pos - vec3(0.0, .000001, 0.0));
				float zStep2 = distanceEstimator(pos - vec3(0.0, 0.0, .000001));
				
				return normalize(vec3(xStep1 - xStep2, yStep1 - yStep2, zStep1 - zStep2));
			}
			
			
			
			vec3 computeShading(vec3 pos, int iteration)
			{
				vec3 surfaceNormal = getSurfaceNormal(pos);
				
				vec3 lightDirection = normalize(lightPos - pos);
				
				float dotProduct = dot(surfaceNormal, lightDirection);
				
				float lightIntensity = lightBrightness * max(dotProduct, -.4 * dotProduct);
				
				//The last factor adds ambient occlusion.
				vec3 color = getColor(pos) * lightIntensity * max((1.0 - float(iteration) / float(maxMarches)), 0.0);
				
				
				
				//Apply fog.
				return mix(color, fogColor, 1.0 - exp(-distance(pos, cameraPos) * fogScaling));
			}
			
			
			
			vec3 raymarch(vec3 startPos)
			{
				//That factor of .9 is important -- without it, we're always stepping as far as possible, which results in artefacts and weirdness.
				vec3 rayDirectionVec = normalize(startPos - cameraPos) * .9;
				
				vec3 finalColor = fogColor;
				
				float epsilon = .0000001;
				
				float t = 0.0;
				
				
				
				for (int iteration = 0; iteration < maxMarches; iteration++)
				{
					vec3 pos = startPos + t * rayDirectionVec;
					
					float distance = distanceEstimator(pos);
					
					//This lowers the detail far away, which makes everything run nice and fast.
					epsilon = max(.0000006, 3.0 * t / float(imageSize));
					
					
					
					if (distance < epsilon)
					{
						finalColor = computeShading(pos, iteration);
						break;
					}
					
					else if (t > clipDistance)
					{
						break;
					}
					
					
					
					t += distance;
				}
				
				
				
				return finalColor;
			}
			
			
			
			void main(void)
			{
				vec3 finalColor = raymarch(imagePlaneCenterPos + rightVec * uv.x * aspectRatioX + upVec * uv.y / aspectRatioY);
				
				gl_FragColor = vec4(finalColor.xyz, 1.0);
			}
		`,canvasWidth:500,canvasHeight:500,worldCenterX:-.2391,worldCenterY:-1.6925,useFullscreen:!0,trueFullscreen:!0,useFullscreenButton:!0,enterFullscreenButtonIconPath:"/graphics/general-icons/enter-fullscreen.png",exitFullscreenButtonIconPath:"/graphics/general-icons/exit-fullscreen.png",switchFullscreenCallback:this.changeResolution.bind(this),mousedownCallback:this.onGrabCanvas.bind(this),touchstartCallback:this.onGrabCanvas.bind(this),mousedragCallback:this.onDragCanvas.bind(this),touchmoveCallback:this.onDragCanvas.bind(this),mouseupCallback:this.onReleaseCanvas.bind(this),touchendCallback:this.onReleaseCanvas.bind(this)};this.wilson=new Wilson(canvas,t),this.wilson.render.initUniforms(["aspectRatioX","aspectRatioY","imageSize","cameraPos","imagePlaneCenterPos","forwardVec","rightVec","upVec","focalLength","lightPos","scaleCenter","n1","n2","n3","n4","numNs","rotationMatrix1","rotationMatrix2"]),this.calculateVectors(),this.imageWidth>=this.imageHeight?(this.wilson.gl.uniform1f(this.wilson.uniforms.aspectRatioX,this.imageWidth/this.imageHeight),this.wilson.gl.uniform1f(this.wilson.uniforms.aspectRatioY,1)):(this.wilson.gl.uniform1f(this.wilson.uniforms.aspectRatioX,1),this.wilson.gl.uniform1f(this.wilson.uniforms.aspectRatioY,this.imageWidth/this.imageHeight)),this.wilson.gl.uniform1i(this.wilson.uniforms.imageSize,this.imageSize),this.wilson.gl.uniform3fv(this.wilson.uniforms.cameraPos,this.cameraPos),this.wilson.gl.uniform3fv(this.wilson.uniforms.imagePlaneCenterPos,this.imagePlaneCenterPos),this.wilson.gl.uniform3fv(this.wilson.uniforms.lightPos,this.lightPos[this.polyhedronIndex]),this.wilson.gl.uniform3fv(this.wilson.uniforms.scaleCenter,this.scaleCenter[this.polyhedronIndex]),this.wilson.gl.uniform3fv(this.wilson.uniforms.forwardVec,this.forwardVec),this.wilson.gl.uniform3fv(this.wilson.uniforms.rightVec,this.rightVec),this.wilson.gl.uniform3fv(this.wilson.uniforms.upVec,this.upVec),this.wilson.gl.uniform1f(this.wilson.uniforms.focalLength,this.focalLength),this.wilson.gl.uniform3fv(this.wilson.uniforms.n1,this.n1[this.polyhedronIndex]),this.wilson.gl.uniform3fv(this.wilson.uniforms.n2,this.n2[this.polyhedronIndex]),this.wilson.gl.uniform3fv(this.wilson.uniforms.n3,this.n3[this.polyhedronIndex]),this.wilson.gl.uniform3fv(this.wilson.uniforms.n4,this.n4[this.polyhedronIndex]),this.wilson.gl.uniform1i(this.wilson.uniforms.numNs,this.numNs[this.polyhedronIndex]),this.wilson.gl.uniformMatrix3fv(this.wilson.uniforms.rotationMatrix1,!1,[1,0,0,0,1,0,0,0,1]),this.wilson.gl.uniformMatrix3fv(this.wilson.uniforms.rotationMatrix2,!1,[1,0,0,0,1,0,0,0,1]);addTemporaryListener({object:window,event:"resize",callback:()=>this.changeResolution()}),window.requestAnimationFrame(this.drawFrame.bind(this))}drawFrame(timestamp){var t=timestamp-this.lastTimestamp;this.lastTimestamp=timestamp,0==t||(this.pan.update(t),this.zoom.update(t),this.moveUpdate(t),this.wilson.worldCenterY=Math.min(Math.max(this.wilson.worldCenterY,.01-Math.PI),-.01),this.theta=-this.wilson.worldCenterX,this.phi=-this.wilson.worldCenterY,this.wilson.render.drawFrame(),this.animationPaused)||window.requestAnimationFrame(this.drawFrame.bind(this))}distanceEstimator(x,y,z){for(let r=0;r<this.numSierpinskiIterations;r++){var t=RaymarchApplet.dotProduct([x,y,z],this.n1[this.polyhedronIndex]),t=(t<0&&(x-=2*t*this.n1[this.polyhedronIndex][0],y-=2*t*this.n1[this.polyhedronIndex][1],z-=2*t*this.n1[this.polyhedronIndex][2]),RaymarchApplet.dotProduct([x,y,z],this.n2[this.polyhedronIndex])),t=(t<0&&(x-=2*t*this.n2[this.polyhedronIndex][0],y-=2*t*this.n2[this.polyhedronIndex][1],z-=2*t*this.n2[this.polyhedronIndex][2]),RaymarchApplet.dotProduct([x,y,z],this.n3[this.polyhedronIndex])),t=(t<0&&(x-=2*t*this.n3[this.polyhedronIndex][0],y-=2*t*this.n3[this.polyhedronIndex][1],z-=2*t*this.n3[this.polyhedronIndex][2]),4<=this.numNs[this.polyhedronIndex]&&(t=RaymarchApplet.dotProduct([x,y,z],this.n4[this.polyhedronIndex]))<0&&(x-=2*t*this.n4[this.polyhedronIndex][0],y-=2*t*this.n4[this.polyhedronIndex][1],z-=2*t*this.n4[this.polyhedronIndex][2]),x),i=y,o=z,e=[[Math.cos(this.rotationAngleZ1),-Math.sin(this.rotationAngleZ1),0],[Math.sin(this.rotationAngleZ1),Math.cos(this.rotationAngleZ1),0],[0,0,1]],n=[[Math.cos(this.rotationAngleY1),0,-Math.sin(this.rotationAngleY1)],[0,1,0],[Math.sin(this.rotationAngleY1),0,Math.cos(this.rotationAngleY1)]],s=[[1,0,0],[0,Math.cos(this.rotationAngleX1),-Math.sin(this.rotationAngleX1)],[0,Math.sin(this.rotationAngleX1),Math.cos(this.rotationAngleX1)]],a=RaymarchApplet.matMul(RaymarchApplet.matMul(e,n),s);x=a[0][0]*t+a[0][1]*i+a[0][2]*o,y=a[1][0]*t+a[1][1]*i+a[1][2]*o,z=a[2][0]*t+a[2][1]*i+a[2][2]*o,t=x=this.scale*x-(this.scale-1)*this.scaleCenter[this.polyhedronIndex][0],i=y=this.scale*y-(this.scale-1)*this.scaleCenter[this.polyhedronIndex][1],o=z=this.scale*z-(this.scale-1)*this.scaleCenter[this.polyhedronIndex][2],e=[[Math.cos(this.rotationAngleZ2),-Math.sin(this.rotationAngleZ2),0],[Math.sin(this.rotationAngleZ2),Math.cos(this.rotationAngleZ2),0],[0,0,1]],n=[[Math.cos(this.rotationAngleY2),0,-Math.sin(this.rotationAngleY2)],[0,1,0],[Math.sin(this.rotationAngleY2),0,Math.cos(this.rotationAngleY2)]],s=[[1,0,0],[0,Math.cos(this.rotationAngleX2),-Math.sin(this.rotationAngleX2)],[0,Math.sin(this.rotationAngleX2),Math.cos(this.rotationAngleX2)]],x=(a=RaymarchApplet.matMul(RaymarchApplet.matMul(e,n),s))[0][0]*t+a[0][1]*i+a[0][2]*o,y=a[1][0]*t+a[1][1]*i+a[1][2]*o,z=a[2][0]*t+a[2][1]*i+a[2][2]*o}return Math.sqrt(x*x+y*y+z*z)*Math.pow(this.scale,-this.numSierpinskiIterations)}changeResolution(resolution=this.imageSize){this.imageSize=Math.max(100,resolution),this.wilson.fullscreen.currentlyFullscreen?1<=aspectRatio?(this.imageWidth=this.imageSize,this.imageHeight=Math.floor(this.imageSize/aspectRatio)):(this.imageWidth=Math.floor(this.imageSize*aspectRatio),this.imageHeight=this.imageSize):(this.imageWidth=this.imageSize,this.imageHeight=this.imageSize),this.wilson.changeCanvasSize(this.imageWidth,this.imageHeight),this.imageWidth>=this.imageHeight?(this.wilson.gl.uniform1f(this.wilson.uniforms.aspectRatioX,this.imageWidth/this.imageHeight),this.wilson.gl.uniform1f(this.wilson.uniforms.aspectRatioY,1)):(this.wilson.gl.uniform1f(this.wilson.uniforms.aspectRatioX,1),this.wilson.gl.uniform1f(this.wilson.uniforms.aspectRatioY,this.imageWidth/this.imageHeight)),this.wilson.gl.uniform1i(this.wilson.uniforms.imageSize,this.imageSize),window.requestAnimationFrame(this.drawFrame.bind(this))}updateParameters(newRotationAngleX1,newRotationAngleY1,newRotationAngleZ1,newRotationAngleX2,newRotationAngleY2,newRotationAngleZ2){var t=this;anime({targets:t,rotationAngleX1:newRotationAngleX1,rotationAngleY1:newRotationAngleY1,rotationAngleZ1:newRotationAngleZ1,rotationAngleX2:newRotationAngleX2,rotationAngleY2:newRotationAngleY2,rotationAngleZ2:newRotationAngleZ2,duration:1e3,easing:"easeOutQuad",update:t.updateMatrices.bind(t)})}updateMatrices(){var t=[[Math.cos(this.rotationAngleZ1),-Math.sin(this.rotationAngleZ1),0],[Math.sin(this.rotationAngleZ1),Math.cos(this.rotationAngleZ1),0],[0,0,1]],i=[[Math.cos(this.rotationAngleY1),0,-Math.sin(this.rotationAngleY1)],[0,1,0],[Math.sin(this.rotationAngleY1),0,Math.cos(this.rotationAngleY1)]],o=[[1,0,0],[0,Math.cos(this.rotationAngleX1),-Math.sin(this.rotationAngleX1)],[0,Math.sin(this.rotationAngleX1),Math.cos(this.rotationAngleX1)]],e=RaymarchApplet.matMul(RaymarchApplet.matMul(t,i),o);this.wilson.gl.uniformMatrix3fv(this.wilson.uniforms.rotationMatrix1,!1,[e[0][0],e[1][0],e[2][0],e[0][1],e[1][1],e[2][1],e[0][2],e[1][2],e[2][2]]),t=[[Math.cos(this.rotationAngleZ2),-Math.sin(this.rotationAngleZ2),0],[Math.sin(this.rotationAngleZ2),Math.cos(this.rotationAngleZ2),0],[0,0,1]],i=[[Math.cos(this.rotationAngleY2),0,-Math.sin(this.rotationAngleY2)],[0,1,0],[Math.sin(this.rotationAngleY2),0,Math.cos(this.rotationAngleY2)]],o=[[1,0,0],[0,Math.cos(this.rotationAngleX2),-Math.sin(this.rotationAngleX2)],[0,Math.sin(this.rotationAngleX2),Math.cos(this.rotationAngleX2)]],e=RaymarchApplet.matMul(RaymarchApplet.matMul(t,i),o),this.wilson.gl.uniformMatrix3fv(this.wilson.uniforms.rotationMatrix2,!1,[e[0][0],e[1][0],e[2][0],e[0][1],e[1][1],e[2][1],e[0][2],e[1][2],e[2][2]]),window.requestAnimationFrame(this.drawFrame.bind(this))}async changePolyhedron(newPolyhedronIndex){await changeOpacity(this.wilson.canvas,0),this.polyhedronIndex=newPolyhedronIndex,this.cameraPos=this.startingCameraPos[this.polyhedronIndex],this.wilson.worldCenterX=-this.startingAngle[this.polyhedronIndex][0],this.wilson.worldCenterY=-this.startingAngle[this.polyhedronIndex][1],this.wilson.gl.uniform3fv(this.wilson.uniforms.lightPos,this.lightPos[this.polyhedronIndex]),this.wilson.gl.uniform3fv(this.wilson.uniforms.scaleCenter,this.scaleCenter[this.polyhedronIndex]),this.wilson.gl.uniform3fv(this.wilson.uniforms.n1,this.n1[this.polyhedronIndex]),this.wilson.gl.uniform3fv(this.wilson.uniforms.n2,this.n2[this.polyhedronIndex]),this.wilson.gl.uniform3fv(this.wilson.uniforms.n3,this.n3[this.polyhedronIndex]),this.wilson.gl.uniform3fv(this.wilson.uniforms.n4,this.n4[this.polyhedronIndex]),this.wilson.gl.uniform1i(this.wilson.uniforms.numNs,this.numNs[this.polyhedronIndex]),window.requestAnimationFrame(this.drawFrame.bind(this)),changeOpacity(this.wilson.canvas,1)}}export{KaleidoscopicIFSFractal};