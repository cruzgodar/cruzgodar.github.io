import anime from"/scripts/anime.min.js";import{getVectorGlsl}from"/scripts/applets/applet.min.js";import{RaymarchApplet}from"/scripts/applets/raymarchApplet.min.js";const nTetrahedron=[[-.57735,0,.816496],[.288675,-.5,.816496],[.288675,.5,.816496]],scaleCenterTetrahedron=[0,0,1],nCube=[[1,0,0],[0,1,0],[0,0,1]],scaleCenterCube=[.57735,.57735,.57735],nOctahedron=[[.707107,0,.707107],[0,.707107,.707107],[-.707107,0,.707107],[0,-.707107,.707107]],scaleCenterOctahedron=[0,0,1],maxIterations=22;function getDistanceEstimatorGlsl(shape,useForGetColor=!1){return`
		${useForGetColor?"vec3 color = vec3(1.0, 1.0, 1.0); float colorScale = .5;":""}
		//We'll find the closest vertex, scale everything by a factor of 2 centered on that vertex (so that we don't need to recalculate the vertices), and repeat.
		for (int iteration = 0; iteration < maxIterations; iteration++)
		{
			//Fold space over on itself so that we can reference only the top vertex.
			float t1 = dot(pos, n1${shape});
			
			if (t1 < 0.0)
			{
				pos -= 2.0 * t1 * n1${shape};
				${useForGetColor?"color = mix(color, color1, colorScale);":""}
			}
			
			float t2 = dot(pos, n2${shape});
			
			if (t2 < 0.0)
			{
				pos -= 2.0 * t2 * n2${shape};
				${useForGetColor?"color = mix(color, color2, colorScale);":""}
			}
			
			float t3 = dot(pos, n3${shape});
			
			if (t3 < 0.0)
			{
				pos -= 2.0 * t3 * n3${shape};
				${useForGetColor?"color = mix(color, color3, colorScale);":""}
			}

			${"Octahedron"===shape?`
				float t4 = dot(pos, n4${shape});
				
				if (t4 < 0.0)
				{
					pos -= 2.0 * t4 * n4${shape};
					${useForGetColor?"color = mix(color, color4, colorScale);":""}
				}
			`:""}
			
			pos = rotationMatrix1 * pos;
			
			//Scale the system -- this one takes me a fair bit of thinking to get. What's happening here is that we're stretching from a vertex, but since we never scale the vertices, the four new ones are the four closest to the vertex we scaled from. Now (x, y, z) will get farther and farther away from the origin, but that makes sense -- we're really just zooming in on the tetrahedron.
			pos = scale * pos - (scale - 1.0) * scaleCenter${shape};
			
			pos = rotationMatrix2 * pos;

			${useForGetColor?"colorScale *= .5;":""}
		}
		
		return ${useForGetColor?"color":"length(pos) * pow(1.0/scale, float(maxIterations))"};
	`}class KaleidoscopicIFSFractal extends RaymarchApplet{rotationAngleX1=0;rotationAngleY1=0;rotationAngleZ1=0;rotationAngleX2=0;rotationAngleY2=0;rotationAngleZ2=0;constructor({canvas}){super({canvas:canvas,distanceEstimatorGlsl:`
			float distanceToScene = 0.0;

			if (tetrahedronAmount > 0.0)
			{
				distanceToScene += tetrahedronAmount * distanceEstimatorTetrahedron(pos);
			}

			if (cubeAmount > 0.0)
			{
				distanceToScene += cubeAmount * distanceEstimatorCube(pos);
			}

			if (octahedronAmount > 0.0)
			{
				distanceToScene += octahedronAmount * distanceEstimatorOctahedron(pos);
			}

			return distanceToScene;
		`,getColorGlsl:`
			vec3 color = vec3(0.0, 0.0, 0.0);

			if (tetrahedronAmount > 0.0)
			{
				color += tetrahedronAmount * getColorTetrahedron(pos);
			}

			if (cubeAmount > 0.0)
			{
				color += cubeAmount * getColorCube(pos);
			}

			if (octahedronAmount > 0.0)
			{
				color += octahedronAmount * getColorOctahedron(pos);
			}

			return color;
		`,addGlsl:`
			const int maxIterations = ${maxIterations};
			
			const vec3 color1 = vec3(1.0, 0.0, 0.0);
			const vec3 color2 = vec3(0.0, 1.0, 0.0);
			const vec3 color3 = vec3(0.0, 0.0, 1.0);
			const vec3 color4 = vec3(1.0, 1.0, 0.0);
			
			const vec3 n1Tetrahedron = ${getVectorGlsl(nTetrahedron[0])};
			const vec3 n2Tetrahedron = ${getVectorGlsl(nTetrahedron[1])};
			const vec3 n3Tetrahedron = ${getVectorGlsl(nTetrahedron[2])};
			const vec3 scaleCenterTetrahedron = ${getVectorGlsl(scaleCenterTetrahedron)};

			const vec3 n1Cube = ${getVectorGlsl(nCube[0])};
			const vec3 n2Cube = ${getVectorGlsl(nCube[1])};
			const vec3 n3Cube = ${getVectorGlsl(nCube[2])};
			const vec3 scaleCenterCube = ${getVectorGlsl(scaleCenterCube)};

			const vec3 n1Octahedron = ${getVectorGlsl(nOctahedron[0])};
			const vec3 n2Octahedron = ${getVectorGlsl(nOctahedron[1])};
			const vec3 n3Octahedron = ${getVectorGlsl(nOctahedron[2])};
			const vec3 n4Octahedron = ${getVectorGlsl(nOctahedron[3])};
			const vec3 scaleCenterOctahedron = ${getVectorGlsl(scaleCenterOctahedron)};

			float distanceEstimatorTetrahedron(vec3 pos)
			{
				${getDistanceEstimatorGlsl("Tetrahedron")}
			}

			float distanceEstimatorCube(vec3 pos)
			{
				${getDistanceEstimatorGlsl("Cube")}
			}

			float distanceEstimatorOctahedron(vec3 pos)
			{
				${getDistanceEstimatorGlsl("Octahedron")}
			}

			vec3 getColorTetrahedron(vec3 pos)
			{
				${getDistanceEstimatorGlsl("Tetrahedron",!0)}
			}

			vec3 getColorCube(vec3 pos)
			{
				${getDistanceEstimatorGlsl("Cube",!0)}
			}

			vec3 getColorOctahedron(vec3 pos)
			{
				${getDistanceEstimatorGlsl("Octahedron",!0)}
			}
		`,uniforms:{tetrahedronAmount:["float",0],cubeAmount:["float",0],octahedronAmount:["float",1],scale:["float",2],rotationMatrix1:["mat3",[1,0,0,0,1,0,0,0,1]],rotationMatrix2:["mat3",[1,0,0,0,1,0,0,0,1]]},theta:.2004,phi:1.6538,cameraPos:[-1.7346,-.4485,.2596],lightPos:[-50,-70,100],lightBrightness:1.35,epsilonScaling:.75})}distanceEstimatorSingular(x,y,z,ns,scaleCenter){var t=this.uniforms.scale[1];for(let c=0;c<maxIterations;c++){var e=RaymarchApplet.dotProduct([x,y,z],ns[0]),e=(e<0&&(x-=2*e*ns[0][0],y-=2*e*ns[0][1],z-=2*e*ns[0][2]),RaymarchApplet.dotProduct([x,y,z],ns[1])),e=(e<0&&(x-=2*e*ns[1][0],y-=2*e*ns[1][1],z-=2*e*ns[1][2]),RaymarchApplet.dotProduct([x,y,z],ns[2])),e=(e<0&&(x-=2*e*ns[2][0],y-=2*e*ns[2][1],z-=2*e*ns[2][2]),4<=ns.length&&(e=RaymarchApplet.dotProduct([x,y,z],ns[3]))<0&&(x-=2*e*ns[3][0],y-=2*e*ns[3][1],z-=2*e*ns[3][2]),x),o=y,n=z,a=[[Math.cos(this.rotationAngleZ1),-Math.sin(this.rotationAngleZ1),0],[Math.sin(this.rotationAngleZ1),Math.cos(this.rotationAngleZ1),0],[0,0,1]],r=[[Math.cos(this.rotationAngleY1),0,-Math.sin(this.rotationAngleY1)],[0,1,0],[Math.sin(this.rotationAngleY1),0,Math.cos(this.rotationAngleY1)]],s=[[1,0,0],[0,Math.cos(this.rotationAngleX1),-Math.sin(this.rotationAngleX1)],[0,Math.sin(this.rotationAngleX1),Math.cos(this.rotationAngleX1)]],i=RaymarchApplet.matMul(RaymarchApplet.matMul(a,r),s);x=i[0][0]*e+i[0][1]*o+i[0][2]*n,y=i[1][0]*e+i[1][1]*o+i[1][2]*n,z=i[2][0]*e+i[2][1]*o+i[2][2]*n,e=x=t*x-(t-1)*scaleCenter[0],o=y=t*y-(t-1)*scaleCenter[1],n=z=t*z-(t-1)*scaleCenter[2],a=[[Math.cos(this.rotationAngleZ2),-Math.sin(this.rotationAngleZ2),0],[Math.sin(this.rotationAngleZ2),Math.cos(this.rotationAngleZ2),0],[0,0,1]],r=[[Math.cos(this.rotationAngleY2),0,-Math.sin(this.rotationAngleY2)],[0,1,0],[Math.sin(this.rotationAngleY2),0,Math.cos(this.rotationAngleY2)]],s=[[1,0,0],[0,Math.cos(this.rotationAngleX2),-Math.sin(this.rotationAngleX2)],[0,Math.sin(this.rotationAngleX2),Math.cos(this.rotationAngleX2)]],x=(i=RaymarchApplet.matMul(RaymarchApplet.matMul(a,r),s))[0][0]*e+i[0][1]*o+i[0][2]*n,y=i[1][0]*e+i[1][1]*o+i[1][2]*n,z=i[2][0]*e+i[2][1]*o+i[2][2]*n}return Math.sqrt(x*x+y*y+z*z)*Math.pow(t,-maxIterations)}distanceEstimator(x,y,z){let t=0;var e=this.uniforms.tetrahedronAmount[1],o=this.uniforms.cubeAmount[1],n=this.uniforms.octahedronAmount[1];return 0<e&&(t+=e*this.distanceEstimatorSingular(x,y,z,nTetrahedron,scaleCenterTetrahedron)),0<o&&(t+=o*this.distanceEstimatorSingular(x,y,z,nCube,scaleCenterCube)),0<n&&(t+=n*this.distanceEstimatorSingular(x,y,z,nOctahedron,scaleCenterOctahedron)),t}updateMatrices(){var t=[[Math.cos(this.rotationAngleZ1),-Math.sin(this.rotationAngleZ1),0],[Math.sin(this.rotationAngleZ1),Math.cos(this.rotationAngleZ1),0],[0,0,1]],e=[[Math.cos(this.rotationAngleY1),0,-Math.sin(this.rotationAngleY1)],[0,1,0],[Math.sin(this.rotationAngleY1),0,Math.cos(this.rotationAngleY1)]],o=[[1,0,0],[0,Math.cos(this.rotationAngleX1),-Math.sin(this.rotationAngleX1)],[0,Math.sin(this.rotationAngleX1),Math.cos(this.rotationAngleX1)]],n=RaymarchApplet.matMul(RaymarchApplet.matMul(t,e),o);this.setUniform("rotationMatrix1",[n[0][0],n[1][0],n[2][0],n[0][1],n[1][1],n[2][1],n[0][2],n[1][2],n[2][2]]),t=[[Math.cos(this.rotationAngleZ2),-Math.sin(this.rotationAngleZ2),0],[Math.sin(this.rotationAngleZ2),Math.cos(this.rotationAngleZ2),0],[0,0,1]],e=[[Math.cos(this.rotationAngleY2),0,-Math.sin(this.rotationAngleY2)],[0,1,0],[Math.sin(this.rotationAngleY2),0,Math.cos(this.rotationAngleY2)]],o=[[1,0,0],[0,Math.cos(this.rotationAngleX2),-Math.sin(this.rotationAngleX2)],[0,Math.sin(this.rotationAngleX2),Math.cos(this.rotationAngleX2)]],n=RaymarchApplet.matMul(RaymarchApplet.matMul(t,e),o),this.setUniform("rotationMatrix2",[n[0][0],n[1][0],n[2][0],n[0][1],n[1][1],n[2][1],n[0][2],n[1][2],n[2][2]]),this.needNewFrame=!0}async changePolyhedron(newAmounts,instant){const t=[this.uniforms.tetrahedronAmount[1],this.uniforms.cubeAmount[1],this.uniforms.octahedronAmount[1]],e={t:0};anime({targets:e,t:1,duration:instant?0:1e3,easing:"easeOutQuad",update:()=>{this.setUniform("tetrahedronAmount",t[0]*(1-e.t)+newAmounts[0]*e.t),this.setUniform("cubeAmount",t[1]*(1-e.t)+newAmounts[1]*e.t),this.setUniform("octahedronAmount",t[2]*(1-e.t)+newAmounts[2]*e.t),this.needNewFrame=!0},complete:()=>{this.setUniform("tetrahedronAmount",newAmounts[0]),this.setUniform("cubeAmount",newAmounts[1]),this.setUniform("octahedronAmount",newAmounts[2]),this.needNewFrame=!0}})}}export{KaleidoscopicIFSFractal};