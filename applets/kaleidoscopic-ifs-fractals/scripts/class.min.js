import{getVectorGlsl}from"/scripts/applets/applet.min.js";import{dotProduct,getRotationMatrix,mat3TimesVector,RaymarchApplet}from"/scripts/applets/raymarchApplet.min.js";import{changeOpacity}from"/scripts/src/animation.min.js";import{clamp}from"/scripts/src/utils.min.js";const minScale=1.125,minScaleEpsilon=3e-5,maxScaleEpsilon=3e-7,bailoutTolerance=.001,ns={tetrahedron:[[-.57735,0,.816496],[.288675,-.5,.816496],[.288675,.5,.816496]],cube:[[1,0,0],[0,1,0],[0,0,1]],octahedron:[[.707107,0,.707107],[0,.707107,.707107],[-.707107,0,.707107],[0,-.707107,.707107]],dodecahedron:[[.809016,-.5,.309017],[.309017,.809016,-.5],[-.5,.309017,.809016],[.809016,-.5,.309017],[.309017,.809016,-.5],[-.5,.309017,.809016],[.809016,-.5,.309017]],icosahedron:[[0,1,0],[-.5,-.309017,.809016],[.5,-.309017,.809016],[-.809016,.5,.309017],[.809016,.5,.309017],[0,1,0]]},scaleCenters={tetrahedron:[0,0,1],cube:[.57735,.57735,.57735],octahedron:[0,0,1],dodecahedron:[.57735,.57735,.57735],icosahedron:[0,.525731,.850651]};function getDistanceEstimatorGlsl(shape,useForGetColor=!1){const e=shape.charAt(0).toUpperCase()+shape.slice(1);var t=ns[shape].length;return`
		${useForGetColor?"vec3 color = vec3(1.0, 1.0, 1.0); float colorScale = .5;":""}
		//We'll find the closest vertex, scale everything by a factor of 2 centered on that vertex (so that we don't need to recalculate the vertices), and repeat.
		for (int iteration = 0; iteration < ${useForGetColor?8:72}; iteration++)
		{
			if (iteration >= numIterations)
			{
				break;
			}

			${Array(t).fill(0).map((_,i)=>`
			float t${i} = dot(pos, n${i}${e});
			
			if (t${i} < 0.0)
			{
				pos -= 2.0 * t${i} * n${i}${e};
			}
		`).join("\n")}
			
			//Scale the system -- this one takes me a fair bit of thinking to get. What's happening here is that we're stretching from a vertex, but since we never scale the vertices, the four new ones are the four closest to the vertex we scaled from. Now (x, y, z) will get farther and farther away from the origin, but that makes sense -- we're really just zooming in on the tetrahedron.
			pos = scale * pos - (scale - 1.0) * scaleCenter${e};
			
			pos = rotationMatrix * pos;

			${useForGetColor?"float r = length(pos); color = mix(color, abs(pos.yxz / r), colorScale); colorScale *= .2;":""}
		}
		
		return ${useForGetColor?"color":"length(pos) * pow(1.0 / scale, float(numIterations))"};
	`}class KaleidoscopicIFSFractals extends RaymarchApplet{shape="octahedron";constructor({canvas,shape="octahedron",epsilonScalingFactor=.6,minEpsilon,theta=.2004,phi=1.6538,resolution=1e3,xrFramebufferScaleSlider}){var e=[];for(const s in ns){const o=s.charAt(0).toUpperCase()+s.slice(1);var t=Array(ns[s].length).fill(0).map((_,i)=>`
					const vec3 n${i}${o} = ${getVectorGlsl(ns[s][i])};
				`).join("\n")+`
				const vec3 scaleCenter${o} = ${getVectorGlsl(scaleCenters[s])};
			`;e.push(t)}var a=e.join("\n");super({canvas:canvas,resolution:resolution,distanceEstimatorGlsl:getDistanceEstimatorGlsl(shape),getColorGlsl:getDistanceEstimatorGlsl(shape,!0),uniformsGlsl:`
			uniform float scale;
			uniform mat3 rotationMatrix;
			uniform int numIterations;
		`,addGlsl:a,uniforms:{scale:2,rotationMatrix:[[1,0,0],[0,1,0],[0,0,1]],numIterations:12},theta:theta,phi:phi,sceneOrigin:[-2.702,-.731,.347],lightPos:[-50,-70,100],lightBrightness:1.4,epsilonScalingFactor:epsilonScalingFactor,minEpsilon:minEpsilon,overstepFactor:1.1,xrFramebufferScaleSlider:xrFramebufferScaleSlider,coneMarchingScale:8}),this.shape=shape}changeScale(scale){var e=(scale-minScale)/(2-minScale)*Math.log10(minScaleEpsilon/maxScaleEpsilon),t=Math.floor(44-12*(scale-8/3)/(8/3-2));this.setUniforms({scale:scale,minEpsilon:minScaleEpsilon/Math.pow(10,e),numIterations:t})}changeRotationAngles(x,y,z){this.setUniforms({rotationMatrix:getRotationMatrix(x,y,z)})}drawFrame(){super.drawFrame();var e=this.distanceEstimator(this.sceneOrigin[0],this.sceneOrigin[1],this.sceneOrigin[2]),t=2*(1/(this.uniforms.scale-1)-1)/7,e=clamp(Math.floor(14-2*Math.log(e)),14,24)+Math.round(28*t);this.uniforms.numIterations!==e&&this.setUniforms({numIterations:e})}distanceEstimator(x,y,z){var t=ns[this.shape??"octahedron"],a=scaleCenters[this.shape??"octahedron"],s=Math.hypot(...a);for(let i=0;i<this.uniforms.numIterations;i++){for(let e=0;e<t.length;e++){var o=dotProduct([x,y,z],t[e]);o<0&&(x-=2*o*t[e][0],y-=2*o*t[e][1],z-=2*o*t[e][2])}x=this.uniforms.scale*x-(this.uniforms.scale-1)*a[0],y=this.uniforms.scale*y-(this.uniforms.scale-1)*a[1],z=this.uniforms.scale*z-(this.uniforms.scale-1)*a[2],[x,y,z]=mat3TimesVector(this.uniforms.rotationMatrix,[x,y,z]);var r=Math.sqrt(x*x+y*y+z*z);if(r>s/bailoutTolerance)return(r-s)*Math.pow(this.uniforms.scale,-(i+1))}return Math.sqrt(x*x+y*y+z*z)*Math.pow(this.uniforms.scale,-this.uniforms.numIterations)}async changePolyhedron(newShape){await changeOpacity({element:this.canvas,opacity:0}),this.shape=newShape;var e=getDistanceEstimatorGlsl(this.shape),t=getDistanceEstimatorGlsl(this.shape,!0);this.reloadShader({distanceEstimatorGlsl:e,getColorGlsl:t}),await changeOpacity({element:this.canvas,opacity:1})}}export{KaleidoscopicIFSFractals};