import{showPage}from"/scripts/src/load-page.min.mjs";!function(){"use strict";var a=$$("pre code");for(let e=0;e<a.length;e++)a[e].classList.add("highlightable");hljs.highlightAll();var e={renderer:"gpu",shader:`
		precision highp float;
		
		varying vec2 uv;
		
		uniform float a;
		uniform float b;
		uniform float brightnessScale;
		
		
		
		void main(void)
		{
			vec2 z = vec2(uv.x * 2.0, uv.y * 2.0);
			vec3 color = normalize(vec3(abs(z.x + z.y) / 2.0, abs(z.x) / 2.0, abs(z.y) / 2.0) + .1 / length(z) * vec3(1.0, 1.0, 1.0));
			float brightness = exp(-length(z));
			
			
			
			vec2 c = vec2(a, b);
			
			for (int iteration = 0; iteration < 100; iteration++)
			{
				if (iteration == 99)
				{
					gl_FragColor = vec4(0.0, 0.0, 0.0, 1.0);
					return;
				}
				
				if (length(z) >= 10.0)
				{
					break;
				}
				
				z = vec2(z.x * z.x - z.y * z.y, 2.0 * z.x * z.y) + c;
				
				brightness += exp(-length(z));
			}
			
			
			gl_FragColor = vec4(brightness / brightnessScale * color, 1.0);
		}
	`,canvasWidth:1e3,canvasHeight:1e3,worldWidth:4,worldHeight:4,worldCenterX:0,worldCenterY:0,useDraggables:!0,draggablesMousemoveCallback:s,draggablesTouchmoveCallback:s};let r=new Wilson($("#output-canvas"),e);r.render.initUniforms(["a","b","brightnessScale"]);r.draggables.add(0,1);let i=0,t=1,n,o=-1,l=$("#resolution-input");function s(e,a,r,n){i=a,t=r,window.requestAnimationFrame(g)}function g(e){var a=e-o;o=e,0!=a&&(r.gl.uniform1f(r.uniforms.a,i),r.gl.uniform1f(r.uniforms.b,t),r.gl.uniform1f(r.uniforms.brightnessScale,10),r.render.drawFrame())}l.addEventListener("input",()=>{n=parseInt(l.value||1e3),r.changeCanvasSize(n,n)}),$("#download-button").addEventListener("click",()=>{r.downloadFrame("a-julia-set.png")}),window.requestAnimationFrame(g),showPage()}();