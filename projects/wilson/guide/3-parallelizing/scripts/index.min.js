import{showPage}from"/scripts/src/load-page.min.mjs";import{$,$$}from"/scripts/src/main.min.mjs";import{Wilson}from"/scripts/wilson.min.mjs";!function(){"use strict";var r=$$("pre code");for(let e=0;e<r.length;e++)r[e].classList.add("highlightable");hljs.highlightAll();var e={renderer:"gpu",shader:`
		precision highp float;
		
		varying vec2 uv;
		
		uniform float a;
		uniform float b;
		uniform float brightnessScale;
		
		
		
		void main(void)
		{
			vec2 z = vec2(uv.x * 2.0, uv.y * 2.0);
			vec3 color = normalize(vec3(abs(z.x + z.y) / 2.0, abs(z.x) / 2.0, abs(z.y) / 2.0) + .1 / length(z) * vec3(1.0, 1.0, 1.0));
			float brightness = exp(-length(z));
			
			
			
			vec2 c = vec2(a, b);
			
			for (int iteration = 0; iteration < 100; iteration++)
			{
				if (iteration == 99)
				{
					gl_FragColor = vec4(0.0, 0.0, 0.0, 1.0);
					return;
				}
				
				if (length(z) >= 10.0)
				{
					break;
				}
				
				z = vec2(z.x * z.x - z.y * z.y, 2.0 * z.x * z.y) + c;
				
				brightness += exp(-length(z));
			}
			
			
			gl_FragColor = vec4(brightness / brightnessScale * color, 1.0);
		}
	`,canvasWidth:1e3,canvasHeight:1e3,worldWidth:4,worldHeight:4,worldCenterX:0,worldCenterY:0,useDraggables:!0,draggablesMousemoveCallback:l,draggablesTouchmoveCallback:l};let a=new Wilson($("#output-canvas"),e);a.render.initUniforms(["a","b","brightnessScale"]);a.draggables.add(0,1);let n=0,o=1,i,t=-1,s=$("#resolution-input");function l(e,r,a,i){n=r,o=a,window.requestAnimationFrame(g)}function g(e){var r=e-t;t=e,0!=r&&(a.gl.uniform1f(a.uniforms.a,n),a.gl.uniform1f(a.uniforms.b,o),a.gl.uniform1f(a.uniforms.brightnessScale,10),a.render.drawFrame())}s.addEventListener("input",()=>{i=parseInt(s.value||1e3),a.changeCanvasSize(i,i)}),$("#download-button").addEventListener("click",()=>{a.downloadFrame("a-julia-set.png")}),window.requestAnimationFrame(g),showPage()}();