// eslint-disable-next-line max-len
const raymarchControlsData = "<div id=\"raymarch-controls-card\" class=\"card\" tabindex=\"0\"><div id=\"card-close-button\"><span>×</span></div><h1 class=\"heading-text\">Controls</h1><p class=\"body-text\">Drag on the scene to look around. Uncheck the Lock on origin checkbox to move the camera freely — on a touchscreen, hold with two fingers to move forward and three to move back, and on a keyboard, use the following control scheme:</p><p class=\"body-text\"><code>WASD</code>: Move forward, backward, left, and right.</p><p class=\"body-text\">Space and Shift: Move up and down.</p><p class=\"body-text\"><code>Z</code>: Zoom in.</p><p class=\"body-text\"><code>F</code>: Toggle fullscreen.</p></div>";

export function raymarchControls()
{
	return raymarchControlsData;
}